# CLAUDE.md "Active focus" history archive

> Moved out of CLAUDE.md on 2026-06-10 to cut context load. Newest first. Per-session detail also lives in memory/project_session_*.md files.

**Active focus (2026-06-02 evening LATEST — 3.9.1 follow-up SHIPPED + live-verified):** Live `new.tapadum.com` = core **3.9.1** + webmcp **1.0.40**. origin/master (luwi-press) = **`2a4f1c0`** (ZIP sha `179b0111d9dd6f2d`, core-only). Two operator-driven follow-ups on top of 3.9.0: (1) **Events settings folded into the Site hub** — new "Events" tab after Vendors (`admin/site-hub-page.php` tool+group); `admin/events-page.php` rewritten self-contained + REST-save (POST /events/settings, mirrors vendors-page.php) so it renders+saves inside the hub include; removed the Events module's own settings submenu/render/load-hook; `luwipress-events` 301s into the hub via `$compat_redirects`; the Events CPT keeps its own top-level add/edit menu. (2) **Bot Defense → Accounts UX** — the "delete not working" report was NOT a bug: all 10 remaining accounts are `protected:realistic_name` (3.2.7 safety tier; 5702 deleted historically). `bot-accounts/list` now returns the LIVE protection verdict (re-scored), and the UI badges protected rows ("Protected: reason"), disables their checkbox, and skips them in Select-all (was silently skipping → looked broken). Files `class-luwipress-bot-account-cleaner.php` + `admin/bot-defense-page.php` (both clean). Live-verified: status 3.9.1 + list returns `protected:true` for all 10. luwipress.php hunk-staged (version + compat lines; connectors `@@ -141` left unstaged). PHPStan [OK] + preflight clean. Lesson: a "feature is broken" report can be a by-design safety guard with no UI feedback — diagnose with a live dry-run before assuming a code bug, then fix the *visibility*, not the guard. See `feedback_cpt_engine_preset_and_wpml_autoconfig.md` + `project_session_2026_06_02_cpt_engine_phase4_5.md`.

**Active focus (2026-06-02 evening — CPT Engine Phase 4 + Phase 5 SHIPPED + live-verified):** Live `new.tapadum.com` = core **3.9.0** + webmcp **1.0.40**. origin/master (luwi-press) = **`ccce4fc`** (`5d919bf` → `22369ad` Phase 4+5 → `ccce4fc` FEATURES doc). Two phases in one session, same rigor (code → 2 independent adversarial reviews → fix → static gates [php -l + PHPStan OK + preflight 0-new] → surgical commit → push → operator upload → live verify). **Phase 4 — Events = CPT Engine preset #2.** New `includes/class-luwipress-events.php` + `admin/events-page.php` (self-contained submenu, NOT in shared settings-page.php); mirrors `LuwiPress_Vendors`. `lwp_event` CPT + `lwp_event_category` tax, **DORMANT by default** (`enabled=0`; enable re-registers CPT+meta+flush in the same request). Structured meta (start/end datetime, status, attendance, venue, online/ticket urls, price/currency, organizer/performer JSON vendor-refs). **Schema reuse, not a new type:** `wire_event_schema()` injects `auto_data` into the builtin Schema Registry `event` type (one Event node, no double-emit, manual `_luwipress_schema_event` still wins, returns `array()` for non-lwp_event posts = zero regression). `render_event` extended (additive) for **multiple organizer/performer**. Vendor bridge = published `lwp_vendor` refs (WC-independent). **ICS** via `?lwp_ics=1` (template_redirect p1, floating-local datetime — regex parse, NO strtotime/gmdate tz shift) + REST `/events/{id}/ics` + MCP `event_ics`. REST `/events/*` (public reads gated publish-or-cap; write bails when disabled). WebMCP 1.0.40: `event_settings_get/set`, `events_list`, `event_meta_set`, `event_ics`. **Phase 5 — WPML/Polylang auto-config, NO 3rd-party DB writes.** KEY: **Polylang reads `wpml-config.xml` too** → one static `luwipress/wpml-config.xml` (extended with Events preset) makes presets translatable in both, zero config. `wpml_config_array` runtime filter undocumented/unstable → NOT used. CPT Engine adds `pll_get_post_types`/`pll_get_taxonomies` filters (enabled engine types → Polylang auto, incl. operator CPTs) + generator `build_wpml_config()/build_wpml_config_xml()` + REST `GET /cpt-engine/wpml-config` + MCP `cpt_wpml_config_get` (pasteable XML for operator CPTs on WPML — operator applies). Parity fix: `_lwp_vendor_entity_type` added to Vendors field_schema (`in_elementor=false`) so generator==static; reverted a risky `_lwp_vendor_primary_group` term_id copy. **Live-verified:** test event 37162 → exactly 1 Event JSON-LD on the page (**LiteSpeed strips the `<!-- LuwiPress schema -->` comment** — grep JSON-LD content, not the comment), 2-performer array, no tz shift, valid `.ics`; generator includes lwp_event only after enable; **status endpoint served STALE — cache-bust live version checks.** Events left ENABLED, test event trashed. Surgical commit hunk-staged luwipress.php (awk-drop `@@ -141` connectors hunk + `git apply --cached`); **concurrent WP7-Connectors refactor STILL uncommitted/untouched** in the shared tree. **CPT Engine roadmap now complete through Phase 5** — next = new scope. ⚠️ Test vendor "Mehmet Usta" (36649) still present (delete before prod demo); stray luwi-themes `master` branch pending delete OK. See `feedback_cpt_engine_preset_and_wpml_autoconfig.md` + `project_session_2026_06_02_cpt_engine_phase4_5.md`. Resume via `.claude/next-session-prompt.md`.

**Active focus (2026-06-02 late — CPT Engine Phase 2 + 3 + WPML refinement SHIPPED + verified):** Live `new.tapadum.com` = core **3.8.2** + webmcp **1.0.39** + theme `luwipress-gold` **1.10.6**. origin/master (luwi-press) = **`5d919bf`**; luwi-themes **main** = **`b9d1444`** (consolidated 1.8.3→1.10.6 multi-session backlog). Three ships, each: adversarial review → fix → independent re-verify → surgical commit (connectors refactor never touched; `luwipress.php` hunk-staged to version lines) → push → operator upload → live verify. (a) **`2cb9206` — core 3.8.0 + theme 1.10.6: CPT Engine Phase 2.** Engine CPTs surfaced in Translation Manager (`luwipress_translatable_post_types` filter + `$tax_types`/`$content_types` extension in `admin/translation-page.php`, engine taxonomies use the same WPML `total>0` gate as core — no no-op buttons) + new generic **`lwp-cpt-grid`** Elementor widget (`themes/luwipress-gold-elementor/inc/widgets/lib/class-cpt-grid.php`, reads engine registry, reuses `.lwp-mgrid` CSS, graceful fallback; `lwp-master-grid` untouched). (b) **`78a0f42` — core 3.8.1: Phase 3.** WC attribution → first-class hidden `product_<cpt>` taxonomy (Vendors→`product_lwp_vendor`): term mirror per CPT post, **bidirectional dual-write** (meta↔terms, `$syncing` try/finally guard), **cron backfill** (never inline — review caught the frontend-retry-storm + the WP_Error-deletes-canonical-meta bug), admin products filter, `meta_box_cb:false`. **Meta stays canonical** (theme REGEXP/KG/FR-016 untouched); taxonomy is an additive WC-native index. ALL engine-level — zero vendors changes (Vendors opts in via its existing `woocommerce.attribution_meta`). (c) **`5d919bf` — core 3.8.2: WPML dedup.** Mirror terms keyed to WPML/Polylang **source-language** post (`source_post_id()`) → one term per vendor not per language; one-time cron dedup collapsed 89 sibling terms → 23 (counts intact, Feramis 36633→4 = FR-016 match). **Live verification caught the WPML sibling explosion that static review missed** — always verify term/row COUNTS on the real multilingual site. See `feedback_cpt_attribution_taxonomy_pattern.md` + `project_session_2026_06_02_cpt_engine_phase2_3.md`. **NEXT = Phase 4** (Events preset CPT #2 + vendor↔event organizer/performer bridge + ICS export, reuse Schema Registry 'event' type → **bump core 3.9.0**). **Phase 5** = WPML auto-config / wpml-config.xml generation (unblocks full TM taxonomy visibility for operator-defined CPTs — today only `lwp_vendor_group` shows because it's already in Tapadum's wpml-config). **Concurrent "remove WP7 Connectors" refactor STILL uncommitted** in the shared tree (rode along in every core ZIP per operator "tam ship"; integrity-clean, not in origin) — leave alone; surgical-commit only your files. ⚠️ Stray luwi-themes `master` branch (= same commit as `main`, accidental) pending operator OK to delete. Resume via `.claude/next-session-prompt.md`.

**Active focus (2026-06-02 — 3.7.4/3.7.5 SHIPPED + CPT Engine Phase 0/1 committed):** Live `new.tapadum.com` = core **3.7.5** + webmcp **1.0.39** (260 MCP tools). origin/master = **`4f7c361`**. Two ships: (a) `0371103` — **3.7.4** admin design-system cohesion (Usage/Translation/Scheduler + standalone tool pages → canonical `lp-header`/`lp-hub-tabs`/`luwipress-card`; Translation drift/sync palette tokenized, 4→3 severities) + **8 MCP tools** (health_score_get, content_ai_tell_audit/_bank, crm_settings_get/set, crm_refresh_segments, kg_candidate_snooze/dismiss) + **3.7.5** per-group vendor URLs (`lwp_vendor_group` archive_slug field + `_primary_group` canonical permalink + dynamic rewrites + resolver-skip filter) + `single_slug_pattern` normalization (the reported "MCP %-clip bug" was actually a stray trailing `'` — verified byte-by-byte on the wire; plugin path is %-safe). (b) `4f7c361` — **CPT Engine Phase 0/1 (DORMANT, no version bump):** new `LuwiPress_CPT_Engine` (`includes/class-luwipress-cpt-engine.php`) generic CPT registry — type-definition directory + operator-defined CPT registration from definitions (post_type+taxonomies+field-schema-meta+permalink; presets skipped via `self_registers`; collision-guarded), REST `/cpt-engine/types` CRUD, MCP `cpt_types_list/cpt_type_set/cpt_type_delete`; Vendors = preset #1 (self-describes via `register_with_cpt_engine`). **Strategic frame (operator + Tapadum):** "vendor" is a misnomer — it's a generic Person/Org/LocalBusiness *actor* model; the real need is a **generic CPT engine** where operators ADD CPTs (Vendors/Team/Events), **translate them (in the Translations page incl. taxonomies)**, and **show them via an Elementor widget**. Events = future preset (today schema-only, not a CPT). Build-vs-detect verdict: HYBRID (own registry + optional ACF/Meta Box sync). WC verdict: promote `_lwp_vendor_ids` JSON-meta → first-class hidden `product_<cpt>` taxonomy. **NEXT = Phase 2** (visible value): Translation Manager surfacing (engine CPT taxonomies → "Translate Taxonomies" step in `admin/translation-page.php`) + Elementor generic "CPT Grid" widget (theme `luwipress-gold-elementor`); then Phase 3 WC taxonomy promotion, Phase 4 Events preset + vendor bridge + ICS, Phase 5 WPML auto-config. **Bump core → 3.8.0 (new module) when Phase 2 lands.** Concurrent "remove WP7 Connectors" refactor STILL uncommitted in the shared tree — leave alone; surgical-commit only your files; hunk-stage only your luwipress.php block. Resume via `.claude/next-session-prompt.md` + `memory/project_session_2026_06_02_cpt_engine_and_3_7_5.md`.

**Active focus (2026-06-01 — 3.7.3 admin UI cohesion + Site-hub grouping SHIPPED + pushed):** Live `new.tapadum.com` = core **3.7.3**. origin/master = **`64e2fbd`** (6 commits this session: merge → flat → 5-group → sub-tabs → taxonomy-asset-fix → baseline-cleanup). Uploaded ZIP `releases/luwipress-v3.7.3.zip` sha **`9b41951f7ecf1ade`**. **Shipped:** (1) Content hub folded INTO the Site hub (sidebar 7→6; `luwipress-content` + all tab deep-links resolve onto Site via unchanged `$compat_redirects` + a hub→hub `?tab=` passthrough). (2) Site hub = **5 grouped tabs** (Content / SEO & Taxonomy / Security / Theme / Vendors) rendered via a **secondary SUB-TAB strip** (`.lwp-group-subtabs`) — the active tool's page renders DIRECTLY, one tool per request. **NOT group-level accordions** (would nest with tools that already have their own accordions + co-loading heavy pages is slow). Operator rejected two earlier iterations (two-row SITE/CONTENT labels; co-load/lazy accordions) before sub-tabs landed — see `feedback_hub_group_subtabs_not_accordions.md`. (3) **Leaf-level accordion rollout** (the accordions the operator LIKES, INSIDE a tool): Vendors / Slug Resolver / Theme / Settings AI + General → `details.lp-collapse` stacks. (4) Accordion summary **focus-ring fix** (browser near-black → branded inset `:focus-visible`). (5) **Taxonomy hub asset fix** — its separate CSS/JS weren't enqueued in the hub + the `.luwipress-tax-editor` scope wrapper was dropped in hub mode → cramped toolbar + dead editor; both fixed (`feedback_hub_tool_asset_enqueue_and_scope.md`). (6) Dropped 2 stale PHPStan baseline ignores for the deleted connectors file (blocked build after cache clear). **Process:** whole 3.7.3 lived at ONE version across ~6 rebuilds; operator re-uploaded each via Hostinger file-replace (admin.css filemtime cache-bust makes same-version CSS refresh). **🔧 IN-FLIGHT (parallel sessions — leave alone):** operator confirmed 2026-06-01 there are **2 other Claude sessions** working this repo concurrently; one is doing an INTENTIONAL "remove WP 7.0 Connectors" refactor (connectors.php DELETED + 13 files modified, uncommitted in the working tree — that's why this UI session saw it appear with no reflog reset). Operator: "refactor devam etsin" → keep it, do NOT revert. It baked into the shipped ZIP (`9b41951f`, no connectors) but isn't in origin (`64e2fbd`) yet; the owning session commits it when done. Integrity verified (0 dangling refs, lint clean). **When multiple sessions share this working tree: only `git add` your OWN files explicitly, never `git add -A`/`git add luwipress/`, and never revert files you didn't change.** **Deferred:** Cookie Consent own-top-tab (operator floated "ayıralım"; currently a Security sub-tab, un-nested); `taxonomy-editor.css` hardcoded-hex → dark-mode tokenize. Resume via `.claude/next-session-prompt.md`.

**Active focus (2026-05-27 — Sprint 1A + 1B SHIPPED in-tree, NOT committed, NOT deployed):** Single-context delivery of the dual-path product strategy backbone. Live state on `new.tapadum.com` UNCHANGED from previous session (core **3.5.3** + webmcp **1.0.34** + theme `luwipress-gold` **1.10.3**). The work below is in working tree only — operator authorization pending for commit + version bump (3.5.3 → 3.5.5 core, 1.0.34 → 1.0.35 webmcp). **The strategic frame Özgür and I locked:** LuwiPress has two UX paths — WebMCP power users (imperative tools) and CodeCanyon mass market (gamified KG-driven UI). Sprint 1 + 1A + 1B build the second path. **Sprint 1 — Content Health Score rubric (`includes/class-luwipress-health-score.php`, ~960 LOC):** new singleton with 6 pillars (SEO Coverage 25% / AEO Coverage 20% / Translation Health 15% / Schema Coverage 15% / Brand Voice 10% / Content Depth 10%), 15min transient cache, full REST surface (`/health/score`, `/health/pillars`, `/health/reset`), settings tab in Settings → Content Health configuring per-pillar weight + target + action threshold. KG `build_summary()` now injects `summary.health_score`; `renderStoreHealthHero()` reads PHP composite first, falls back to legacy JS formula. **Hotfix `taxonomy_seo_meta_bulk`:** new REST `POST /luwipress/v1/taxonomy/seo-meta-bulk` (in `class-luwipress-api.php`) + WebMCP `taxonomy_seo_meta_bulk` tool. Mirrors post `seo_meta_bulk` for taxonomy terms (each row `{term_id, taxonomy, title?, description?, focus_keyword?}`, 500-row cap). Unblocks Sprint 2 Multi-lang Taxonomy Editor — 52 cat × 4 lang × 3 fields = 624 sequential `taxonomy_meta_set` → 1 bulk call. **Hotfix homepage migration replay:** `temp/tapadum/replay_homepage_migration_to_translations.py` + `HOMEPAGE_MIGRATION_REPLAY_PLAYBOOK.md` for fixing the FR/IT/ES homepage skeleton-strip bug (EN's 2026-05-23 widget migration didn't propagate to translation siblings 19978/16660/24975). Script wraps `/elementor/sync-structure` with `preserve_text=true`, auto-snapshots each target. **NOT YET RUN against live KLON** — pending operator. **Sprint 1A — visible quick-win surfaces:** `LuwiPress_Content_Audit` extended with AI-tell phrase bank (5 langs en/tr/fr/it/es, §1.8 blacklist from Tapadum SEO yazım rehberi — "In the world of...", "stands as one of the most...", "In conclusion," etc.) + 2 new REST endpoints (`/content/ai-tell-audit`, `/content/ai-tell-bank`). 3 new admin pages: **Content Audit** (`admin/content-audit-page.php`, ~390 LOC + inline JS, 3-tab unified UI: Promo Phrases / AI-Tells / Word Count with hero stat row for Brand Voice + Content Depth pillars, severity-banded findings with edit links), **Schema Preview** (`admin/schema-preview-page.php`, ~260 LOC, Schema Registry overview + URL inspector + Rich Results Test handoff, replaces chrome-devtools-mcp round-trips for non-WebMCP customers), **Translation Sync** (deep-link redirect submenu → `?tab=sync-audit` on Translations page; auto-opens the `<details>` panel + scrolls into view). New per-CPT word count target settings in AI Content tab — drives the Content Depth pillar's measurement function. Defaults: product 500-650-800, post 1200-1500-2200, page 300-400-600 (§1.10 Tapadum SEO yazım rehberi). **Sprint 1B — FAQ Tab Editor (`includes/class-luwipress-faq-editor.php`, ~320 LOC + 4 asset files):** inline metabox on product/post/page (filter-extensible) edit screens with row repeater, AI generate button (proxies `/aeo/generate-faq` async pipeline), word count badges per row (50-80 green, 30-49/81-110 amber), status pill (empty/manual/completed/processing/failed), reorder via ↑/↓ buttons, schema preview cross-link. Save through standard `save_post` hook (no extra REST round-trip): inputs carry `luwipress_faq_q[]` + `luwipress_faq_a[]` parallel arrays, handler reassembles canonical `[{question, answer}, ...]` shape, writes via `update_post_meta(_luwipress_faq, $rows)`. **3 legacy storage formats accepted on read** (canonical array / serialized array / JSON string — covers 92. otrm legacy + every MCP write path). Hard cap 20 rows. Empty rows dropped silently. Full clear → meta deleted entirely (no orphan empty FAQPage schema). Cache invalidation chain on save: `luwipress_aeo_coverage` transient + Health Score cache + Plugin Detector post cache. This is **the single biggest non-WebMCP UI gap closed** — until now `_luwipress_faq` was MCP/REST-only writable. **`translation-page.php` minor patch:** added `?tab=sync-audit` query param support — auto-adds `open` attribute to the sync-audit `<details>` + scrolls into view. Pairs with the new Translation Sync submenu. **Architectural learnings folded into memory candidates (operator may surface as feedback files):** (a) single-context Sprint integration over multi-session splits — 3500+ LOC delivered cleanly in one context; (b) admin metabox over Gutenberg block for universal classic + Gutenberg + custom-admin-theme compatibility; (c) read-tolerant write-canonical for legacy meta format migration (FAQ editor accepts 3 shapes, writes 1); (d) settings tab as KG scoring rubric (not just config) — settings drive the Health Score, which drives the Action Queue cards, which drives operator behavior. **Build artifacts:** none yet — uncommitted in-tree only. **Pending operator (resume sequence):** (a) verify FAQ Editor on KLON manually before commit (edit one Tapadum product, confirm metabox renders + saves), (b) run homepage migration replay against KLON live, (c) authorize commit + version bumps, (d) build ZIPs + deploy. **Commit pattern (when authorized):** single range commit `feat(core 3.5.3 → 3.5.5, webmcp 1.0.34 → 1.0.35): Content Health rubric + Sprint 1A surfaces + FAQ Editor + bulk taxonomy SEO meta + Translation Sync hoist`. **Sprint 2 ready to start:** Multi-language Taxonomy Editor (backend bulk dep shipped this session) + Image Alt Bulk. **Sprint 3 planned:** Pilot Pre-Check Wizard + Content Integrity Validator + Custom Schema Picker + Maker Voice detector. Roadmap: yazım rehberinin ~%50'si now UI-accessible; Sprint 2 → ~%70, Sprint 3 → ~%85 target. Resume via `.claude/next-session-prompt.md`.

**Earlier (2026-05-26 late — Vendor-FR-016 fix + homepage shortcode→widget swap + comprehensive people→vendor rename SHIPPED, core 3.5.3 + webmcp 1.0.34 + theme 1.10.3):** **LIVE on new.tapadum.com:** core **3.5.3** + webmcp **1.0.34** + theme `luwipress-gold` **1.10.3**. Two range commits pushed: `66e883d` (3.4.6 → 3.5.2 + webmcp 1.0.33 → 1.0.34: Vendors module + KG vendor x-ray + privacy-gated customer detail + comprehensive `lwp_person → lwp_vendor` rename) and `691f5fb` (3.5.2 → 3.5.3: Vendor-FR-016 bidirectional render robustness fix). **The big architectural lift on top of 3.5.2's Vendors module:** Tapadum tester (Özgür) surfaced FR-016 — Feramis Aktas's vendor profile page rendered empty "work grid" despite the Knowledge Graph reporting `product_count=4`. **Root cause:** `_lwp_vendor_ids` product meta was being written in TWO incompatible JSON shapes from different code paths — canonical `LuwiPress_Vendors::save_product_vendor` emitted `["123"]` (quoted strings via `array_map('strval', $valid) + wp_json_encode`), but Tapadum's seed scripts emitted `[123]` (bare integers via direct update_post_meta). Template's `meta_query` used `sprintf('"%d"', $vendor_id)` LIKE pattern — only matched the quoted shape. Silent failure mode: data present, query misses it; KG counted correctly because its bulk loader uses `json_decode` (shape-agnostic) but the template-side LIKE comparison didn't. **Three-part fix shipped in core 3.5.3 + theme 1.10.3:** (1) **Plugin**: `register_post_meta` for `_lwp_vendor_ids` on `product` post type with new `normalize_product_vendor_ids` sanitize_callback that accepts ANY incoming shape (JSON / comma-list / array / single integer) and validates each ID against an actual published `lwp_vendor` post before emitting canonical `["123","456"]` form. Every future write through `update_post_meta`, REST, MCP `meta_set`, wp-cli, or third-party hooks is normalized at the meta API boundary. (2) **One-time migration**: `maybe_migrate_vendor_ids_format` runs on `init` priority 20 (skips AJAX/cron/REST), walks every product carrying the meta, re-saves through sanitize callback — pre-existing integer-format data normalizes automatically on next page load after upgrade. Idempotent flag in `luwipress_vendor_ids_normalized` option. (3) **Theme**: `single-lwp_vendor.php` + `class-master-grid.php` query switched from `LIKE` to `REGEXP` with JSON-array-element-aware boundaries `(\[|,)[[:space:]]*"?<id>"?[[:space:]]*(,|\])` — tolerates both formats AND `36633` doesn't false-positive against `366331`. Theme-side fix is independent of plugin upgrade. **Companion KG `made_by` edge fix:** vendors-only `/knowledge-graph?sections=vendors` calls now return both counts AND edges from single bulk query (previously: correct counts, zero edges — supply graph was rendering as isolated nodes). **Theme companion 1.10.2 — `lwp-product-grid` widget shipped (luwipress-gold):** Pro-free curated product grid widget — native Elementor controls for limit / columns / orderby / category / featured-only / manual ID list, preserves arbitrary wrapper classes (e.g. `tap-products`) for legacy CSS hooks, renders with `lwp-pcard` chrome for visual consistency. Tapadum homepage post 7690 had ONE remaining `[products limit=8 columns=4 orderby=popularity class="tap-products"]` shortcode widget after the 1.8.0 widgetization milestone — kit JSON `0 HTML` audit only counts `html` widget types, not `shortcode`. Live-swapped via MCP: snapshot `33382d59` → add `lwp-product-grid` widget `219f73f` → delete `tap_prod_grid` → cache purge. Verified: 8 lwp-pcard cards render, tap-products class preserved, no shortcode/legacy leak. **Comprehensive rename `lwp_person → lwp_vendor` (3.5.2):** Previous draft session had inconsistent CPT slug naming. Operator: "lwp_person'ı lwp_vendor yapmıştık tutarlılık sağlayalım aslında vendor çünkü" — confirmed comprehensive rename across CPT slug, 13 meta keys (`_lwp_vendor_*`), class name (`LuwiPress_Vendors`), file name (`class-luwipress-vendors.php`), option prefix (`luwipress_vendors_`), REST namespace (`/vendors/*`), all 6 MCP tools (`vendor_*`), and theme template names (`archive-lwp_vendor.php` + `single-lwp_vendor.php`). Schema.org Person type name (`entity_type === 'person'`) PRESERVED — it's a Schema.org spec value, not a module identifier. **Live demo seed:** Mehmet Usta (Test Atelier) id=36649 created with full profile + attached to product 33872 (Professional Solo Clay Darbuka N.4) — used for live integration smoke test. **Verification post-deploy:** Feramis (was broken) now renders 1 EN card + "1 work" stat; Yildirim (was working) no regression; Mehmet (test) works; KG made_by edges 8 (4 Feramis + 3 Yildirim + 1 Mehmet). **WPML scope semantic:** template scopes "N works" count to current-language siblings (correct behavior — Tapadum tester expected `1 work` per language), while KG `product_count` counts all-language siblings (correct for KG cross-language analytics). Both correct, ask Tapadum on preferences. **Build artifacts:** `releases/luwipress-v3.5.2.zip` (sha `da1af65c83332385`, 811 KB) + `luwipress-v3.5.3.zip` (sha `ac6ca26a2f1031bb`, 814 KB) + `luwipress-webmcp-v1.0.34.zip` (sha `10f96c2f9015d351`, 110 KB) + `luwipress-gold-1.10.1.zip` (sha `91abe686933fe11e`, 626 KB) + `luwipress-gold-1.10.2.zip` (sha `9f875a77de9f6e29`, 630 KB) + `luwipress-gold-1.10.3.zip` (sha `002ef2b0378f1106`, 630 KB). **Pending operator (cleanup):** (a) test vendor "Mehmet Usta (Test Atelier)" (id=36649) deletion before any production demo; (b) `class-luwipress-elementor.php` working-tree has uncommitted `lwp-contact-cta` translation registry entry — minor, ship with next bump. **Tapadum reply sent for FR-016 closure** — awaiting WPML scope preference + new FR round (Rank Math `manufacturer` field JSON-LD gap is queued separately as the schema-side complement). Resume via `.claude/next-session-prompt.md`.

**Earlier (2026-05-26 — Vendor / Luthier CPT shipped, core 3.5.1 + webmcp 1.0.34 + theme 1.10.0):** **LIVE on new.tapadum.com:** core **3.5.1** + webmcp **1.0.34** (235 MCP tools) + theme `luwipress-gold` **1.10.0** + new generic `LuwiPress_People` module (`includes/class-luwipress-people.php`, ~720 LOC) + `lwp_person` CPT (rendered as "Luthier / Luthiers" via per-site settings) + 4 luthier posts published + Person Schema.org JSON-LD live. **Strategic milestone:** first generic, configurable CPT in LuwiPress core — same module powers music store's luthier directory, restaurant's chef page, gallery's artist roster. Internal post type identifier `lwp_person` stable; rewrite slug + UI labels + entity_type (organization|person|localbusiness) configurable per site. **The big architectural lift:** Schema_Registry-backed Person/Organization/LocalBusiness JSON-LD emission with verified `sameAs` social URLs (FB/IG/YT/SC/LI/X/Behance/Website) — strong E-E-A-T signal for Google. WC integration: product admin sidebar metabox (`Vendor selector`) + PDP `woocommerce_single_product_summary` p25 "Made by [Vendor]" line + `woocommerce_structured_data_product` filter enrichment with `manufacturer` (Org/LB) or `author` (Person). 5 REST endpoints (`/people/settings`, `/people`, `/people/{id}`, `/people/{id}/meta`, `/people/sync-rewrite`) + 6 WebMCP tools (people_settings_get/set, people_list, people_get, people_meta_set, people_flush_rewrite). Theme: `single-lwp_person.php` (~155 LOC portrait + meta strip + social chips + bio + quote + "Their work" WC products grid) + `archive-lwp_person.php` (~110 LOC vendor index grid) + `lwp-master-grid` widget fully refactored (CPT auto-query, removes legacy `items` repeater, optional `include_ids` curation). Tapadum-specific: archive_slug=`luthiers`, label=`Luthier`, entity_type=`person`, 4 luthier posts (Feramis Aktas 36625 / Yildirim Palabiyik 36626 / Hamid 36627 / Golestani 36628) with location + specialty + years + occupation meta set, `/masters/` → 301 → `/luthiers/` legacy redirect (incl. /it/, /fr/, /es/ pairs). Homepage `lwp-master-grid` (id 3b6ad5f) now auto-queries CPT — 4 luthier names render. Person Schema.org confirmed live on `/luthiers/yildirim-palabiyik/` with `homeLocation` + `jobTitle` + `hasOccupation.monthsOfExperience=384` + `knowsAbout: ["Saz", "Tanbur"]` (sameAs empty until operator fills social URLs). **Bug fixes on the way:** (a) Schema_Registry `auto_data` returning empty array short-circuits before renderer — fix: build schema INSIDE auto_data, renderer passthrough (3.5.0 → 3.5.1 patch); see `feedback_schema_registry_auto_data_trap.md`; (b) `instanceof WC_Product` PHPStan failure — codebase uses duck-typing `is_object + method_exists('get_id')` since WC_Product not stubbed in our PHPStan setup; see `feedback_phpstan_wc_product_duck_type.md`; (c) standard WP REST POST `/wp/v2/people` returns 401 with Bearer token — use LuwiPress `content_create_post` MCP tool which accepts arbitrary `post_type`; (d) Earlier in session: info-bar Font Awesome icons not rendering (1.9.0 → 1.9.1 patch) — `function_exists('class::method')` is always false, fix is `class_exists` + `render_icon()` call which also enqueues FA bundle as side effect; also default icon_bg `#d83131` red → empty + CSS default gradient gold. **Pending operator (manual, not blocking):** (a) per-luthier featured image upload (portraits); (b) per-luthier bio writing in WP Admin → Luthiers; (c) social URLs fill (FB/IG/YT/SC/Website) — populate `sameAs` automatically; (d) per-product vendor tick via sidebar metabox — activates "Made by" line + Product Schema `author`. **Build artifacts:** `releases/luwipress-v3.5.1.zip` (sha-16 `f0ffbd5a4cec21f0`, 805 KB), `releases/luwipress-webmcp-v1.0.34.zip` (sha-16 `a025e8aa4c5302e0`, 109 KB), `releases/luwipress-gold-1.10.0.zip` (sha-16 `17618034ec120313`, 626 KB), `temp/tapadum/setup_luthiers_post_deploy.py` (re-runnable data-ops script). **Convention:** any future vendor / maker / atelier / artisan profile work MUST use `LuwiPress_People` instead of building per-theme CPTs. See `feedback_people_cpt_vendor_pattern.md` for full design + `project_session_2026_05_26_people_cpt_3_5_1_ship.md` for ship log. Resume via `.claude/next-session-prompt.md`.

**Earlier (2026-05-23 late — 3.4.1 + theme 1.7.35 + full homepage widget migration):** **LIVE on new.tapadum.com:** core **3.4.1** + webmcp **1.0.32** + theme `luwipress-gold` **1.7.35** + **homepage post 7690 fully widgetized (8/8 sections native Gold widgets)**. Two commits pushed: `03a14cc` (luwi-press master — 3.4.1 sprint: B.1.a Content Audit + B.1.b Frontend Inspector + FR-013 A Category FAQ archive render), `ba82af5` (luwi-themes main — 1.7.33 → 1.7.35: 4 shipping bugs + body-agnostic CTA tier + archive-header lead). **Plugin sprint (3.4.1):** Three modules shipped — `LuwiPress_Content_Audit` (5-language promotional-phrase bank en/tr/fr/it/es, 141 phrases, Unicode-aware word boundaries TR ı/ş/ğ + diacritics, severity ladder meta=high/title-excerpt=medium/body=low for GMC compliance), `LuwiPress_Frontend_Inspector` (4-scope render dump head/content/meta/schema — replaces ~5 chrome-devtools-mcp round-trips per audit), FR-013 A leg closure (category FAQ accordion via 3-hook strategy `term_description` filter + `get_the_archive_description` filter + `woocommerce_archive_description` action, per-term double-render guard). 3 new MCP tools: `content_promotional_phrase_audit`, `content_promotional_phrase_bank`, `lwp_frontend_render_dump`. **Theme bugfix release (1.7.35):** Four shipping bugs closed — (1) homepage products drift right: `inc/mobile-card-width-override.php` ul.products `margin:0 → margin-left/right: calc(50% - 50vw)` classic break-out pattern (Elementor section padding-left 32px was pinning ul at x=32 while width grew to 100vw, bleeding 32px past viewport right); (2) mobile drawer items shift L/R: `.lwp-mobile-drawer .lwp-drw-flat` explicit `box-sizing: border-box` (was rendering 44px wider than drawer body) + `.lwp-mobile-drawer__body` explicit `overflow-x: hidden`; (3) page loader appears AFTER content: `inc/page-loader.php` boot-mark script `data-no-optimize="1" data-no-defer="true" data-cfasync="false"` attrs + `litespeed_optm_js_defer_exc` + `litespeed_optm_js_excludes` filters to block LiteSpeed's `type="litespeed/javascript"` rewrite which was deferring boot-class arrival from p1 to DCL ~900ms; (4) category archive subcat tiles flush to viewport edges on mobile: `.lwp-archive-subcat-grid { padding: 0 12px }` mobile gutter. Plus 1.7.34 polish folded in: body-agnostic CTA tier on `li.product.lwp-pcard` (Elementor WC products widget renders without body.woocommerce class, old gated tiers missed; new tier anchors on theme-stamped class at specificity 0,4,4) + archive-header lead `p/a` styling. **Homepage migration (live REST mutation, no source diff):** 11 raw HTML widgets on post 7690 replaced with 8 Gold custom widgets across 8 sections. Customer commercial-shipping unblocked — they can now edit every homepage section via Elementor's WYSIWYG UI controls, no HTML editing required. Phase 1: Hero (`tap_hero_l/r_html` → `lwp-hero-split` widget `a4cfd71`, section restructure 2col→1col via `bulk-update` `_column_size:100` + `structure:'10'`), Info Bar (`tap_infobar_html` → `lwp-info-bar` widget `117ed97`), Categories (`tap_cat_html` → `lwp-category-grid` widget `08a4608`; one re-add cycle because repeater key was wrong — `categories` instead of `items`). Phase 2 same session: Atelier (`tap_atelier_l/r_html` → `lwp-story-split` widget `30e5d1e`, 2col→1col restructure), Masters (`tap_masters_html` → `lwp-section-head` widget `b290455` + `lwp-master-grid` widget `3b6ad5f` dark variant), Products head (`tap_prod_head` → `lwp-section-head` widget `06ebbca` with 5-pill row + delete `tap_prod_grid_fallback`), YouTube (`tap_yt_head` → `lwp-youtube-channel` widget `6504c45` with 4 placeholder URLs), Journal (`tap_journal_html` → `lwp-editorial-grid` widget `4336567`, auto-queries WP posts). Snapshot rollback handles: `35e65252` (pre-Phase 1), `12137945` (pre-Phase 2). **Three sprint learnings folded into memory:** (a) `feedback_litespeed_inline_script_defer_trap.md` — LS rewrites inline `<script>` `type` to `litespeed/javascript` breaking sync execution; fix is `data-no-optimize="1"` + `litespeed_optm_js_defer_exc` filter belt-and-braces; (b) `feedback_wp_kses_strips_html_comments.md` — HTML comment diagnostic markers silently stripped by `wp_kses_post()`; use structural elements `<span data-debug>` etc. for marker emission inside content paths (cost 3 re-uploads on FR-013 A verify before identification); (c) `feedback_homepage_widget_migration_pattern.md` — 6-step recipe for HTML→widget migration with gotchas (repeater key matching, `bulk_update` accepts arbitrary settings via `texts` dict, heading `[em]xxx[/em]` italic-gold accent token). **Pending operator (next session):** (a) `tapadum.com` (legacy production, real customer traffic) still on 3.3.5 — 3 ZIPs waiting in `releases/`, promotion path TBD; (b) Özgür closure message drafted bilingual but not sent (operator approval pending); (c) Hero `quote_name`/`quote_initial`/`quote_avatar` empty (operator to fill via Elementor UI); (d) YouTube `video` URLs all placeholder (operator to swap to real Tapadum channel URLs); (e) Hero video upload — operator can swap via Elementor UI now Hero Split is widgetized. **Next sprint Tier 1 candidates:** `lwp_translation_render_parity` (~200 LOC, reuses Frontend Inspector parsing), `woo_gmc_compliance_audit` (~250 LOC, builds on phrase bank). Resume via `.claude/next-session-prompt.md`.

**Earlier (2026-05-23 morning — Schema Registry + Tapadum vendor batch SHIPPED, core 3.4.0 + webmcp 1.0.31):** **LIVE on new.tapadum.com:** core **3.4.0** + webmcp **1.0.31**. Two commits pushed to origin/master: `09f3498` (backlog cleanup 3.3.5 + webmcp 1.0.30 — KG metric cards + Next Wins Review fixes + Vendor-BUG-005 HTML preservation) and `d473443` (sprint 3.4.0 + webmcp 1.0.31 — Schema Registry + FR-1/2/012/013 + MCP-3/4, 1431 insertions). Closes all 6 near-term Tapadum vendor asks from 2026-05-22 message in a single minor release. **The big architectural lift: `LuwiPress_Schema_Registry`** ([class file](luwipress/includes/class-luwipress-schema-registry.php)) — generic, context-aware (post OR term), filter-extensible JSON-LD type registry that replaces the hard-coded FAQ/HowTo/Speakable `if/elseif` branches in `LuwiPress_AEO::output_aeo_schema()` with a single emitter. Eight built-in types (FAQPage, HowTo, Speakable, LocalBusiness, Service, Course, Review, AggregateRating) + one auto-generated (ItemList for product_cat archives). Third-party plugins register a 9th type via `luwipress_schema_registry_init` filter — adding "Event" or "Recipe" or any other schema.org type is now config, not code. Backwards-compat trick: legacy AEO emitter at `wp_head` p5 still handles FAQ/HowTo/Speakable on products unchanged; registry at p6 consults `get_aeo_legacy_emissions()` to skip duplicates. Five new REST endpoints under `/aeo/`: `save-schema`, `get-schema`, `delete-schema`, `schema-types`, `schema-render`. Seven new MCP tools: `aeo_save_schema` (generic), `aeo_get_schema`, `aeo_delete_schema`, `aeo_list_schema_types`, `seo_schema_render` (diagnostic — fetch URL with cache-bypass + parse JSON-LD blocks, replaces chrome-devtools-mcp round-trips for schema audits), `wpml_seo_settings_get`, `wpml_seo_settings_set`. **FR-013 — Category FAQ**: `aeo_save_faq` now accepts `term_id`+`taxonomy` (default `product_cat`); writes term meta `_luwipress_faq`; registry renders FAQPage on archive head. Tapadum sweep (52 cats × 4 langs) drops body-embedded `<h3>` workaround. **FR-2 — Auto ItemList**: every product_cat archive emits ItemList with up to 30 products (filter `luwipress_itemlist_product_limit`), per-term 1h cache. **FR-1 — Schema family**: LocalBusiness/Service/Course/Review/AggregateRating via generic `aeo_save_schema` with passthrough sanitizer (recursive URL + kses + structure preserve); unblocks Tapadum Part 5 deferrals from the platform side. **FR-012 — x-default hreflang**: late `wp_head` p99 callback `output_xdefault_supplement` runs AFTER WPML/Polylang and appends missing x-default; works on both singular posts (`wpml_permalink` filter) and taxonomy archives (`wpml_object_id` + brief `wpml_switch_language` context for term_link resolution). **MCP-3 — WPML config**: 10 whitelisted subkeys, walks nested `icl_sitepress_settings` array paths cleanly. **Build script learning**: `build-zip.php` PHPStan memory bump 2G → 4G (single-process worker, `phpstan.neon` already has `maximumNumberOfProcesses: 1`, was crashing at 2G on full luwipress/ tree). **Live verified 2026-05-23**: `/aeo/schema-types` returns 9 types; Clay Darbuka category renders `Organization+WebSite+CollectionPage+ItemList` (was just first 3 pre-3.4.0); `aeo_save_faq term_id=46 taxonomy=product_cat` → FAQPage block appears at p6 then deletes cleanly; category archive hreflang includes en+it+fr+es+**x-default** (was missing pre-3.4.0); `wpml_seo_settings_get` returns full WPML config (default_language=en, url_type=subdir, active=en/it/fr/es). **Build artifacts**: `releases/luwipress-v3.4.0.zip` (776 KB, 77 files, sha-256[:16] `35ff8681349e4a5e`); `releases/luwipress-webmcp-v1.0.31.zip` (104 KB, 8 files, sha-256[:16] `dd1753096b62a3b0`). **Pending operator**: (a) Özgür kapanış mesajı drafted last session (in transcript, search "Özgüre mesaj yazalım") — user asked to plan next sprint before sending; options are send-as-is / tighten-and-send / wait-until-tapadum.com-upload. (b) tapadum.com (legacy production) upload scheduled with DNS swap window. **Next sprint candidates** (from Tapadum 2026-05-22 strategic input doc § 12 priority matrix, remaining after this sprint closed 6): Tier 1 — `content_promotional_phrase_audit` (LOW EFFORT, HIGH FREQUENCY, GMC impact), `lwp_frontend_render_dump` (broader than MCP-4 we shipped — full head+content+meta scopes, replaces 80%+ DevTools usage), `lwp_translation_render_parity` (4-language frontend HTML structural diff), `woo_gmc_compliance_audit` (revenue-impacting). Tier 2 NLP-heavy: passage_audit, entity_extract, question_inventory, compliance_check, post_write_verify_chain, product_seo_health. **Architectural convention**: any future schema rendering work MUST use `LuwiPress_Schema_Registry` instead of touching `LuwiPress_AEO::output_aeo_schema()` directly. See `feedback_schema_registry_pattern.md` for the design pattern + `project_session_2026_05_23_3_4_0_ship.md` for the full ship log. Resume via `.claude/next-session-prompt.md`.

**Earlier (2026-05-20 — Agentic 1.2.0 tool-calling loop SHIPPED + Bot Defense 3.2.7 + Easy-WP-SMTP detection 3.2.8 + luwi.dev infra recovery):** **BUILT + uploaded to new.tapadum.com**: `luwipress-v3.2.7.zip` (sha `0831dc317f3854f8`, 700 KB), `luwipress-v3.2.8.zip` (sha `79a6b4c38b0729bf`, 700 KB), `luwipress-agentic-v1.1.2.zip` (sha `8d60ee66b93e68c6`, 24 KB), `luwipress-agentic-v1.2.0.zip` (sha `ea403a563f8beffc`, 27 KB). **Core 3.2.7** — Bot Account Cleaner gains **3-tier short-circuit protection** in `score_user()`: `has_orders` (existing) + `email_verified` (new — `is_email_verified()` checks 5 known meta keys + `luwipress_user_email_verified` filter) + `realistic_name` (new — `has_realistic_name()` requires both first+last 2-40 chars, vowel-bearing latin+Turkish, no digits, no half-repetition, no keyboard-mash, no 3+ char repeat, distinct from each other + user_login, `luwipress_user_has_realistic_name` filter override). Plus animated bulk sweep UI: new "Sweep matching" button + cursor-paginated `POST /bot-accounts/delete-by-filter` (min_score≥40 floor, limit≤50, after_user_id cursor — OFFSET would skip rows after deletes change positions AND loop forever over skipped/protected; cursor-by-id never has either failure). Per-row animation: pulse-red→fade-out on delete, green+`✓ reason` pill on keep. Pause/Resume/Stop controls. **Core 3.2.8** — `detect_email()` Easy WP SMTP 2.x detection expanded from `defined('EASY_WP_SMTP_VERSION')` to OR-chain that adds `function_exists('easy_wp_smtp')` + class signatures `EasyWPSMTP\Plugin`/`EasyWPSMTP\Core` (SendLayer rebrand has autoloader-first init that sometimes defers the constant). **Agentic 1.1.2** — `LuwiPress_Agent_Adapter_HTTP::normalize_endpoint()` defensive normalizer (auto-appends `/agent` when operator pastes a bare host; preserves custom paths like `/api/v1/chat`); save-flow JS live-updates `#agentic-active-pill` + `#agentic-active-label` + `.claw-sidebar-section .description strong` DOM in-place so changing Active backend requires no page reload. **Agentic 1.2.0 — major feature** — full WebMCP tool-calling agentic loop. `call_ai()` rewritten as multi-turn (max 5) loop: `build_tool_catalog()` reads WebMCP `$tools` registry via Reflection, filters `annotations.readOnlyHint=true`, converts to OpenAI function-calling schema, caps 40 entries, caches 1h transient. `execute_tool_call()` calls WebMCP `$handlers[$name]($args)` directly via Reflection (no HTTP round-trip — same WP process). HTTP adapter passes through `tool` role + `assistant.tool_calls` metadata. **Safety invariant non-negotiable**: only readOnly tools exposed to autonomous LLM invocation; destructive ops (delete, mutate, enrich, retranslate) NEVER picked up by LLM. Result: questions like "kaç müşteri var" / "flagged bot count" / "FAQ coverage" return actual site data instead of "I can't see your site directly". **New infrastructure on `91.99.229.96`** (Luwi's Hetzner box): `luwi-agent-gateway.service` (FastAPI on `127.0.0.1:3140`, systemd-enabled, journald logs, /etc/luwi/agentic.env config), `LUWI_AGENT_GATEWAY_TOKEN=931fb60ffb86245094686b47dc18de3562445d44083473be7241ddb842a980d1` (Bearer token shared by Hermes + Open Claw plugin cards), nginx patches add `location = /agent { proxy_pass http://127.0.0.1:3140/agent }` to BOTH `hermes.luwi.dev.conf` + `oc.luwi.dev.conf` before the catch-all (paperclip-internal + openclaw-gateway web UIs still served via root path). Gateway forwards `tools` to OpenAI Chat Completions, parses native `tool_calls` into compact wire format `{id, function_name, arguments, action, params}` (last two = backward-compat aliases), reshapes assistant.tool_calls back to OpenAI native on history forwarding. Host-aware system prompt switching (Hermes persona vs Open Claw persona). **Paperclip + Sim + OpenClaw recovery** (one-off ops, NOT in shipped plugin): paperclip embedded postgres `pg_hba.conf` trust-pivot + shell builtin kill -HUP (no /bin/kill binary in slim node image) + cascade DELETE user/account/session/verification + orphan-role migration (`instance_user_roles.user_id` + `company_memberships.principal_id` UPDATE from old to new umutsun id, preserving "Luwi Internal" company ownership). Sim Studio 19 FK cascade delete. OpenClaw `OPENAI_API_KEY` env injection + model `openai/gpt-5.5` → `openai/gpt-4o-mini` + image 2026.4.2→2026.5.18 + token rotation. **All 4 luwi.dev services** (agents/hermes/sim/oc) now accessible with `umutsun@gmail.com` / `Luwi!2026Reset` (BetterAuth services) or `14cebb58ac7a8f95f55e252a14aacb66` (OpenClaw token). **Open issue (deferred)**: OpenClaw native chat (web UI + Telegram bot) fails with OpenAI 400 `"Invalid value: 'custom', param: 'tools'"` — Codex helper plugin sends incompatible tool format. Doesn't block plugin testing (plugin goes through luwi-agent-gateway). Two paths: downgrade to 2026.4.2 (no Codex) or `plugins.deny=["codex"]`. **Pending operator**: HUGE git backlog uncommitted (last commit `def8a11` May 18). Per `feedback_deploy_consolidates_backlog.md` consolidate everything into a single range commit when authorized: `feat(core 3.1.59 -> 3.2.8, agentic open-claw 1.0.1 -> agentic 1.2.0, webmcp 1.0.19 -> 1.0.23): Bot Account Cleaner + 3-tier guards + animated sweep + Cookie Consent + Bot Shield + Wordfence delegation + Agentic rebrand + tool-calling loop + Easy WP SMTP detection`. See `project_session_2026_05_20_agentic_120_ship.md` + `feedback_agentic_tool_calling_loop_pattern.md` + `feedback_embedded_postgres_pghba_trust_pivot.md`.

**Earlier (2026-05-18 evening — Security + Compliance triad SHIPPED, core 3.1.59 → 3.2.1):** **BUILT (not yet deployed):** 4 ZIPs in `releases/`: `luwipress-v3.1.60.zip` (sha `c6da974ff72d4c00`, 656 KB — Bot Account Cleaner), `luwipress-v3.2.0.zip` (sha `3455b587f3b4d204`, 686 KB — Cookie Consent + Bot Shield), `luwipress-v3.2.1.zip` (sha `3129e22ff52c62da`, 688 KB — Wordfence-aware delegation), `luwipress-webmcp-v1.0.20.zip` (sha `7d6c5e28633432c0`, 76 KB) + `luwipress-webmcp-v1.0.21.zip` (sha `d74efd22f64b370f`, 79 KB). **Final cumulative pair for upload: 3.2.1 core + 1.0.21 webmcp.** Three new core modules — all DEFAULT-OFF: (1) **Bot Account Cleaner (3.1.60)** — score-based (0-100) detection + safe deletion of fake user accounts. Signals: disposable email domain (200+ blocklist), `+` alias, high-entropy local, 0 orders + 0 comments + never-logged-in + stale age, missing real name, burst registration. Hard-coded safety: admin/editor/shop_manager/author/contributor NEVER scored, any WC customer with order auto-excluded, dry-run by default + server re-scores before delete. DB `wp_luwipress_bot_account_scores`. Admin "LuwiPress → Bot Accounts" + 7 MCP tools. (2) **Cookie Consent (3.2.0)** — GDPR/ePrivacy banner + preferences modal + log + AI policy generator. 3 modes × 4 categories. Vanilla-JS frontend, light/dark/auto, multilingual via WPML/Polylang. Script blocking via `<script type="text/plain" data-luwipress-consent="analytics">…</script>` pattern. IP stored as SHA-256-salted hash only. Public throttle 5/min/IP. AI policy uses `LuwiPress_AI_Engine::dispatch($workflow, $messages, $options)` STATIC method passing detected analytics/marketing/Meta tags. DB `wp_luwipress_cookie_consent_log`. Admin "LuwiPress → Cookie Consent" + 5 MCP tools. (3) **Bot Shield (3.2.0)** — 6-layer front-edge filter at `init` p1: UA blocklist (22 default scrapers), per-IP rate limit on sensitive paths, REST user enum block (`/wp/v2/users` + `?author=N`), XML-RPC kill, honeypot (24h auto-ban), Googlebot/Bingbot reverse-DNS verify. Hard guards: admins + RFC1918 never blocked. Dry-run `/bot-shield/test` previews without firing. DB `wp_luwipress_bot_blocks`. Admin "LuwiPress → Bot Shield" + 7 MCP tools. (4) **Wordfence-aware delegation (3.2.1)** — `LuwiPress_Plugin_Detector::detect_security()` checks `WORDFENCE_VERSION` constant. When Wordfence active, Bot Shield's `delegated_layers()` helper returns `['ua_blocklist', 'rate_limit', 'honeypot']` and edge_filter skips those layers. Cookie consent + REST user enum + XML-RPC kill stay on (Wordfence Free doesn't provide them). Admin green banner shows delegation state. `bot_shield_stats` payload includes `friendly_security` + `delegated_layers`. Override hook: `apply_filters('luwipress_bot_shield_delegated_layers', $layers, $detect)`. **PHPStan trap learned**: `apply_filters('hook', $int, ...)` narrows return to int via PHPDoc inheritance — never use `if (is_array) elseif (is_int)` chain (elseif unreachable). Restructure as single `if (is_array) {} else { (int)$x }`. Also `LuwiPress_AI_Engine` is NOT a singleton — all methods static. See `project_security_compliance_modules_2026_05_18.md` + `feedback_phpstan_apply_filters_typing.md`. **Pending operator**: upload 3.2.1 + 1.0.21 ZIPs via Hostinger to new.tapadum.com first. Test sequence: Bot Accounts → scan + dry-run delete; Cookie Consent → enable opt-in + verify frontend banner; Bot Shield → enable + quick-test `AhrefsBot` + `/wp-login.php`. **Pending commit**: range commit `feat(3.1.59 -> 3.2.1, webmcp 1.0.19 -> 1.0.21): Bot Account Cleaner + Cookie Consent + Bot Shield + Wordfence-aware delegation + 19 MCP tools` per the deploy-consolidates-backlog pattern.

**Earlier (2026-05-18 pm — LuwiPress Emerald theme v1.0.0 SHIPPED):** **BUILT:** `releases/luwipress-emerald-1.0.0.zip` (sha-16 `df9c28939e77bfa4`, 946 KB, **142 files**, lint clean + BOM clean). Source `themes/luwipress-emerald-elementor/`. ZIP slug `luwipress-emerald/`. New sister theme to LuwiPress Gold — same companion plumbing (LuwiPress core + WebMCP + Marketplace Sync + Open Claw) and same **full Gold module surface** (wizard, theme bridge, 23-tool maintenance suite, ecosystem dashboard, AI surface, featured-products registry, mega-menu admin, footer enhancements, page loader, smart filters, slug-collision shim, WC fallbacks, custom Elementor widgets) but re-skinned for **B2B / consulting / agency / knowledge-work** verticals. Built from the Claude Design **Acme / Emerald** handoff bundle (23 reference HTML pages + 6 chrome partials + design system, archived at `temp/Luwipress Emerald Elementor Theme/`). Visual language: **Inter** (sans) + **JetBrains Mono** (numerics), **emerald jewel palette** built around `#047857` primary (emerald-700) / `#065F46` hover (emerald-800) / `#ECFDF5` soft (emerald-50) — **fully overrideable from Customizer → Brand** which writes `--primary` CSS variable into `<head>` via `inc/customizer/output-css.php`. 1280px container, 12-step spacing scale (`--sp-1..--sp-32`), three-tier shadows (rgba tinted with green-cast `rgba(4,47,33,*)` to match palette), sticky shrinking header, left-slide mobile nav, slide-in WC cart drawer, deep-ink CTA band with animated radial sheen, reveal-on-scroll motion with `prefers-reduced-motion` fallback. **Build path**: `php build-theme-zip.php 1.0.0 luwipress-emerald-elementor luwipress-emerald` (build-theme-zip.php already accepts source_slug + output_slug overrides — no script change needed). **Port mechanics**: bulk-copied Gold's `inc/`, `assets/css/`, `assets/js/`, `assets/wizard-css/`, `assets/wizard-js/`, `template-parts/`, `woocommerce/`, `elementor-kit/`, plus `home.php / single-master.php / page-about.php / page-contact.php / elementor_header_footer.php`, then sed-renamed namespace tokens across every PHP/CSS/JS/JSON/MD/TXT file in the tree: `LUWIPRESS_GOLD_*` → `LUWIPRESS_EMERALD_*`, `luwipress_gold_*` → `luwipress_emerald_*`, `lwp_gold_*` → `lwp_emerald_*`, `LWP_GOLD_*` → `LWP_EMERALD_*`, `LuwiPress_Gold_*` → `LuwiPress_Emerald_*`, `LuwiGold` → `LuwiEmerald`, `lwp-gold-*` → `lwp-emerald-*` (CSS classes), `--lwp-gold-*` → `--lwp-emerald-*` (CSS custom props), `--gold,#XXX` → `--primary,#XXX`. After rename: restored over-aggressively-replaced doc strings in `style.css` (description says "Sister to LuwiPress **Gold**" not "Emerald") + `functions.php` (says "Forks the LuwiPress **Gold** backbone"). **Dropped from port (intentional, Gold-specific)**: `assets/css/widgets.css` (274 KB Gold `.lwp-*` widget styles with Cormorant Garamond + gold palette — conflicts with Emerald's `.emerald-*` design system) and `assets/css/woo-overrides.css` (118 KB Tapadum music-store overrides). `inc/enqueue.php` updated to NOT enqueue these — Emerald ships only `tokens.css` for the design system layer. Customizer Brand defaults flipped from Gold (`#735c00`) to Emerald (`#047857`) in `inc/customizer/bootstrap.php`, color list expanded (primary, primary_hover, primary_soft, accent, sale, success, ink, bg, bg_alt, black — 10 tokens). `inc/customizer/output-css.php` rewritten to read the new token list and emit `:root { --primary: …; --primary-hover: …; --primary-soft: …; --accent: …; --sale: …; --success: …; --ink: …; --bg: …; --bg-alt: …; --black: …; }` so any Customizer brand-color change instantly cascades through every `.emerald-*` component. **Screenshot**: `screenshot.png` (1200×900 RGB, 559 KB) rendered via headless chrome `chrome.exe --headless=new --window-size=1200,900 --force-device-scale-factor=1` from the HTML template at `C:/tmp/emerald-screenshot/template.html` — emerald gradient logo mark, "Emerald" wordmark in Inter 88px 600, ecosystem chips in emerald-soft (LuwiPress Core / WebMCP / Marketplace Sync / Open Claw), friendly-with chips in neutral (Elementor / WooCommerce / WPML / Rank Math / FluentCRM / LiteSpeed), palette swatches at bottom showing the emerald jewel palette. **Files shipped (142)**: PHP templates (404, archive, comments, footer, footer-canvas, functions, header, header-canvas, home, index, page, page-about, page-canvas, page-contact, search, searchform, sidebar, single, single-master, elementor_header_footer, template-parts/insight-card + template-parts/journal-card) + `inc/` (29 PHP files: setup, enqueue, elementor-compat, elementor-kit-sync, luwipress-bridge, featured-products, mega-menu-admin, mega-menu-customizer, friendly-plugins, ai-surface, wc-pdp-hooks, wc-pdp-gallery-override, archive-parent-enrichment, smart-filters, wc-page-fallback, blog-page-fallback, template-redirects, layout-fixes, footer-enhancements, page-loader, mobile-card-width-override) + `inc/maintenance/` (8 files: class-elementor-shell-tool, class-maintenance-tools, class-fix-tools, class-extra-audit-tools, class-seo-audit-tools, class-language-drift-tool, elementor-template-force, seo-enforcement) + `inc/customizer/` (bootstrap + output-css) + `inc/wizard/` (bootstrap + lib + views) + `inc/widgets/` (loader + lib + 23 custom Elementor widgets all token-driven so they inherit emerald automatically) + `inc/patterns/` + admin-only `ecosystem-dashboard.php` + `migration.php` + `assets/css/` (tokens.css 38 KB Emerald DS, ai-surface.css, ecosystem-dashboard.css) + `assets/js/` (frontend.js with WC `added_to_cart` cart drawer hook, ai-surface.js, customize-preview.js, loadmore.js, pdp-gallery.js) + `assets/wizard-css/` (notice.css, wizard.css) + `assets/wizard-js/` (notice.js, wizard.js) + `woocommerce/` templates (archive-product, single-product, content-product, cart/, checkout/, myaccount/) + `elementor-kit/` JSONs (12 reference Elementor templates inherited from Gold; operator imports per-page on activation) + `theme.json` + `readme.txt` + `README.md` + `LICENSE` + `.gitignore` + `screenshot.png` + `FEATURES.md` + `SCREENSHOT.md` + `CLAUDE-DESIGN-PROMPT*.md`. **Quality gate**: 107 PHP files all `php -l` clean, 0 BOM bytes. **Reference design bundle** archived at `temp/Luwipress Emerald Elementor Theme/` — 23 HTML pages + 6 chrome partials + Acme-Design-System.html serve as visual reference for Elementor authoring (not shipped in ZIP). **Pending operator (deploy day)**: upload ZIP via Appearance → Themes → Upload, activate, run wizard, Customizer → LuwiPress Emerald → Brand → confirm/override primary color, populate menus (primary, footer-solutions, footer-resources, footer-legal), import any of the 12 elementor-kit JSONs per-page as needed. **Next session candidates**: (1) Elementor kit JSONs derived from the 23 Emerald HTML pages (homepage hero / solutions / journal / team / account suite / cart / checkout) — current kit JSONs are inherited Gold templates; (2) WC template re-skin with `.emerald-*` classes (currently inherits Gold WC templates which use `.lwp-*` classes — they render OK but don't carry the Emerald design tokens; the missing widgets.css means they fall back to WC defaults which still pull `--primary` from our token sheet, so functional but not pixel-matched to the Acme/Emerald PDP design); (3) screenshot v2 with actual rendered homepage if operator wants something more illustrative than the brand mark.

**Earlier (2026-05-18 am — Vendor-BUG-004 fix + homepage CTA/footer polish + DNS swap GO):** **LIVE on new.tapadum.com:** core **3.1.59** + `luwipress-webmcp` **1.0.19** (182 tools) + theme `luwipress-gold` **1.7.33** + Kit CSS V69 overlay (5287 B). **WebMCP 1.0.19 closes Vendor-BUG-004** (reported by Tapadum Hub session 102): `taxonomy_update_term` previously rejected every WPML translation-term update (IT 237 `darbuka-argilla` / FR 298 / ES 362) with "slug already in use" because `wp_update_term()` always re-runs `wp_unique_term_slug()` regardless of which args you pass, and that function isn't WPML-aware — sibling-language terms get falsely flagged as duplicates. Two-layer fix: (a) description-only path bypasses `wp_update_term` entirely via direct `$wpdb->update` on `wp_term_taxonomy` + `clean_term_cache` + `edited_term`/`edited_{taxonomy}` action emission (response carries `method: "direct_description_write"`); (b) full-update path (name/slug/parent) sets `$sitepress->switch_lang($term_lang)` before `wp_update_term` and restores the previous language after, so the slug-uniqueness check scopes to siblings in the term's own language (response carries `method: "wp_update_term"`). Unblocks the 156-term Tapadum category SEO description sweep. Build `releases/luwipress-webmcp-v1.0.19.zip` (sha-16 `eac2050f822206b0`, 74 KB, 7 files, lint clean). Commit `def8a11` pushed to origin/master. **Pre-swap redirect audit verified clean**: full `slug_resolver_redirect_audit` sweep across EN(19)/FR(341)/IT(339)/ES(417) menus returned 216/216 URLs landing HTTP 200, 0 loops, 0 404s, 0 multi-hop chains, 0 issues — **DNS swap green-lit**. **Kit CSS V69 layer addresses 3 homepage concerns**: (i) Elementor WC Products widget ATC pill rendered washed-gray because `widgets.css` rules gate on `body.woocommerce` which the homepage doesn't carry — V69 adds a body-agnostic tier anchored on `li.product.lwp-pcard` (specificity (0,4,4) beats Elementor per-widget overrides); (ii) homepage product card buttons didn't bottom-align because `li.product { display: flex; flex-direction: column; height: 100% }` + `a.lwp-pcard-link { flex: 1 1 auto }` are also body.woocommerce-gated — V69 adds structural rules so all four cards in a row land their buttons at the same Y; (iii) `.lwp-site-footer { margin-top: 96px }` stacked on the last Elementor section's bottom padding produced a ~150-200px cream band above the footer — V69 zeros the margin and locks `html` bg to `var(--black, #0c0c0c)` so any visible space above the topbar or below the footer reads as footer band, not cream flash. Theme source already updated (`themes/luwipress-gold-elementor/assets/css/widgets.css` body-agnostic CTA tier + `tokens.css` margin-top:0 + html bg-black) for next theme bump 1.7.34 — V69 Kit CSS layer can be stripped after that bump. **Tapadum social URLs set live** via `theme_settings_set` MCP: Instagram (`tapadum`) + Facebook (`tapadum.instruments`) + YouTube (`/c/Tapadum`) — 3 SVG pills render under the brand column (logo + tagline). **Translation Sync Audit state**: products 0 findings, posts 0 findings, pages 141 findings (126 structural_gap + 15 outdated, mostly low-severity Elementor section-count drift). Not a swap blocker; 6 high-severity items can sweep next session via `translation_sync_fix`. **Pending operator (swap-day, unblocked)**: A site DB+uploads backup, DNS TTL drop 24-48h before swap, final audit re-run 10min before, `blog_public 0→1` flip immediately before swap, DNS A record swap at Hostinger, `wp search-replace tapadum.com new.tapadum.com` (then reverse), LiteSpeed admin → "Do Not Cache URIs" → add `wp-json/luwipress/v1/`. See `feedback_wpml_term_slug_validation_bypass.md` + `feedback_body_class_anchor_for_elementor_widgets.md`. Resume via `.claude/next-session-prompt.md`.

**Earlier (2026-05-17 — Slug Resolver core promotion + nested-leaf-fallback 404 rescue + mobile uniform 90vw + SEO migration clean):** **LIVE on new.tapadum.com:** core **3.1.58** + `luwipress-webmcp` **1.0.17** (180 tools) + theme `luwipress-gold` **1.7.33** (sha `2feaf2fcc01acf1f`, 535 KB). Commits `97665c3` (luwi-press) + `98b3648` (luwi-themes) pushed. **Shipped core 3.1.56–3.1.58 — Slug Resolver promoted to core**: new `LuwiPress_Slug_Resolver` class (file `includes/class-luwipress-slug-resolver.php`) ports the theme-tier six-pass discovery (exact / WPML cross-language / fuzzy plural-prefix / Levenshtein-1 / menu-parent inheritance / empty-term ancestor fallback) to core so every theme inherits migration-friendly slug-collision rescue. `template_redirect` priority 1 hook + REST surface `/luwipress/v1/slug-resolver/{diag,map,rebuild,override,settings}`. 3.1.57 adds `X-LWP-SR:` response-header trace at every decision-point (entry / map-size / path / lang-stripped / match / target / will-redirect / loop-guard / no-match / no-match-nested / skip-method) — critical for cross-customer migration troubleshooting without server-side log access. 3.1.58 adds nested-leaf-fallback: multi-segment URLs like `/blog/<dead-slug>/` retry the leaf segment in the map, so Google-indexed dead blog posts 301 to the relevant `/product-category/` archive instead of 404 (SEO link equity preserved). **WebMCP 1.0.17** ships 5 MCP tools (`slug_resolver_diag/_map/_force_rebuild/_override_set/_settings_set`) mirroring the REST surface. **Theme 1.7.29–1.7.33**: 1.7.29 `template-redirects.php` compat shim (early-return when `class_exists('LuwiPress_Slug_Resolver')`); 1.7.30 mega-menu walker gains `get_slug_resolver_map()` helper (reads core resolver, legacy theme function fallback) — restores count badges in mega panels on multilingual sites where page items match `product_cat` via WPML cross-language; 1.7.32 `inc/mobile-card-width-override.php` extends mobile 90% to `body.page` + `body.archive .lwp-archive-header`; 1.7.33 switches `width: 90%` → `width: 90vw` (viewport units, parent-padding-independent), UL `100vw` + `grid justify-items:center` so cards center as viewport-wide 90vw blocks regardless of upstream WC core padding rules. **Live data work on new.tapadum.com**: 4 operator slug overrides set (black-sea-kemence / kemence-de-la-mer-noire / kemence-del-mar-nero → term 27 Bowed; healing-effects-of-the-ney → term 58 ney-turkish demonstrates nested-leaf-fallback rescue for `/blog/<dead>/`). **SEO audit via `C:/tmp/seo_audit.py`**: 48 slugs × 4 langs probed with cache-buster GET — 48/48 single-hop 301, hreflang=4 reciprocal each, **issues=0** → DNS swap clean from redirect-chain / canonical / hreflang standpoint. **Mobile uniform 90vw verified** on EN/FR/IT/ES (header + card across home / archive / blog). **Pending operator manual (~5-10 min)**: WP Admin → Görünüm → Menüler → Menu IT (id 339), add 5 product_cat term items: Caglama (35) / Ogur Sazı (39) / Qanun (43) / Riq (49) / Kabak Kemane (56) — `menu_add_item` MCP attempt created orphans due to WPML interference, manual add works because WPML language context is set correctly in admin. Resume via `.claude/next-session-prompt.md`. See `feedback_skip_method_head_vs_get.md` (HEAD probe trap) + `feedback_menu_add_item_wpml_orphan.md` (MCP can't attach across WPML context).

**Earlier (2026-05-16 — Translation Sync Audit module + AEO language detection + FAQ regen sweep):** **LIVE on new.tapadum.com:** core **3.1.55** + `luwipress-webmcp` **1.0.16** (175 tools) + theme `luwipress-gold` **1.7.26** (sha `82edfa4d1b5c9fde`, 529 KB). Commit `8193af5` pushed to origin/master. **Shipped 3.1.54 — Translation Sync Audit module**: new `LuwiPress_Translation_Sync` orchestrator class with 4 cross-language detect-routines (drift wraps `/translation/language-drift` from 3.1.49; outdated wraps `/translation/outdated` from 3.1.41; structural_gap is NEW — compares `_elementor_data` section count between source + translation; schema_parity is NEW — compares `_luwipress_faq`/`_luwipress_howto`/`_luwipress_speakable` across WPML trid siblings). Uniform finding shape `{finding_id, type, severity, source_id, target_id, lang, gap_summary, fix_action, fix_args, metric}`. New REST surface: `GET /translation/sync-audit` + `POST /translation/sync-fix` + `GET/POST /translation/sync-settings`. Fix dispatcher re-resolves findings server-side (no client fix_args trust). KG Action Queue integration via `luwipress_kg_action_queue_external_candidates` filter — top 8 (type,lang) groups become candidates with 1h cache. Opt-in hourly `wp_cron` sweep with optional auto-fix for high-severity (capped 20/tick). Translation Manager admin gains collapsible "Translation Sync Audit" panel (Step-3 slot) with settings + Run-audit + per-finding Fix. **WebMCP 1.0.16** mirrors with `translation_sync_audit` / `translation_sync_fix` / `translation_sync_settings{,_set}` tools. **Shipped 3.1.55 — AEO target_language auto-detect fix**: `LuwiPress_AEO::dispatch_aeo_generation` now reads WPML/Polylang post language and forwards as `target_language` to the prompt builder. Previous behavior fell back to `luwipress_target_language` option (typically "English") → FAQ generation on FR/IT/ES translated posts produced EN content. Discovered when this sprint's 24 translated FAQ regen calls returned English Q&A. Map of supported language codes: en/fr/it/es/de/pt/tr/ar/ja/zh/nl/ru — extend `$lang_names` for more. See `feedback_aeo_target_lang_auto_detect.md`. **FAQ regen sweep** (live data work, no source change for this part): 16 EN products had TR-drift `_luwipress_faq` cleared in an earlier session — this sprint regenerated fresh EN FAQ via `aeo_generate_faq` (5 Q&A each, high quality, product-specific facts like Tuning Light System mechanics). Then 24 audit-confirmed translated drift targets got fresh FR/IT/ES FAQ after 3.1.55 deploy (sample-verified 6/6: "Qu'est-ce qu'une darbuka", "Qual è il materiale", "¿Qué es un bouzouki"). **Final sync-audit on new.tapadum.com**: drift=0, outdated=0, structural_gap=0, schema_parity=100 (capped at limit; ~267 actual). The schema_parity findings are sibling-of-EN-FAQ gaps — 89 EN-FAQ products (73 pre-existing healthy + 16 fresh) each have 0-3 missing FR/IT/ES sibling FAQ. OUT OF SCOPE this sprint per operator decision; next sprint sweeps via `aeo_generate_faq` × sibling_id now that target-lang detection is automatic. Also shipped 3.1.53 (FAQPage JSON-LD double-emit fix — wp_footer p20 duplicate emitter removed; canonical wp_head p5 retained) and webmcp 1.0.15 (taxonomy_meta_get/set/delete tools for TASK-K-002 batch Rank Math meta). Theme bumps 1.7.23→1.7.26 (PDP mobile bottom gap fix specificity, then mobile thumb scrollbar fix, then desktop thumb scrollbar fix; ZIP slug `luwipress-gold-1.7.26.zip`). See `project_translation_sync_audit_3_1_54_ship.md` + `feedback_aeo_faq_double_emit.md` + `feedback_aeo_target_lang_auto_detect.md`. Resume via `.claude/next-session-prompt.md`.

**Concurrent thread (2026-05-12 night — customer chat clarify/follow-up chip layer + global localization, pre-upload):** On top of the live 3.1.52 deploy, a parallel chat workstream rebuilt the 3.1.52 ZIP (final on-disk sha **`fa91be04f4878038`**, 617 KB) adding: (a) AI JSON envelope `{reply, chip_kind, chips:[3]}` + JS chip pill rendering below assistant bubbles (clarify/follow_up rows); (b) KG-grounded chip selection — prompt block `KG SIGNALS:` lists new_arrivals/top_rated/best_ready/segment/viewing (from `page_url` parse); (c) typo detection via metaphone+Levenshtein dual-gate ("buziki"→"Bouzouki Greek", "kemence"→"Kamancheh"), gated on empty product search; (d) full AI-translated localization pack — canonical `CHIP_VOCAB_EN` (18 keys) + `localize_chips($lang)` reads `wp_options.luwipress_chat_chips_<lang>` cache + wp_cron async translator + auto-warmer reads WPML/Polylang active langs on every chat asset render; (e) admin REST `POST /chat/warm-translations {lang?, force?}` for manual warm/re-translate. **Live verification 2026-05-12 evening confirmed chip_pack present on EN/FR/IT/ES**, identified 3 quality issues (IT anglicism "top-rated" kept, FR+IT `other_options` placeholder grammar broken, FR tu/vous inconsistent). **Fixes in on-disk ZIP `fa91be04f4878038`**: refined canonical EN templates (`Top-rated %s`→`Best %s`, `Other %s options`→`More from %s`) + tightened translation prompt (ban anglicisms, force ONE register, allow word-order adjust around %s). **Resume action on operator upload**: hit EN homepage and verify `lpChat.chip_pack.top_rated === "Best picks"` (confirms new ZIP live), then `POST /chat/warm-translations {"force": true}` to delete cached FR/IT/ES packs and trigger re-translation under improved prompt; ~30-60s wait for wp-cron; re-audit via PowerShell fetch script. **Note**: chat CSS `.lp-chat-toggle` size 56→46 change is currently in the working tree (operator UI feedback) but NOT in the on-disk 3.1.52 ZIP — held for 3.1.53. See `project_chat_3_1_52_chips_localization.md` + feedback memories `feedback_chat_clarify_chip_layer_pattern.md` / `feedback_chat_ui_localization_via_ai_cache.md` / `feedback_chip_template_translation_friendliness.md` / `feedback_typo_detection_metaphone_levenshtein.md`.

**Active focus (2026-05-12 — Tapadum migration recovery, DNS-swap ready):** **LIVE on new.tapadum.com:** core **3.1.52** + `luwipress-webmcp` **1.0.14** (sha `1b8ab77d257bd025`, 64 KB) + theme `luwipress-gold` 1.7.10. **A→B migration completed end-to-end.** Recovery session closed all gaps from 2026-05-11 partial port: hreflang-based WPML translation discovery resolved 56 sibling posts that the wrong `wp/v2/{type}/{id}?lang=es` method had missed; 85 records ported (69 OK + 16 swap-day URL-host artefacts); 50 product inline FAQ stripped from `post_content` (FAQ tab from `_luwipress_faq` meta preserved); 15 FAQPage duplications cleaned via `meta_delete _luwipress_faq` (old multi-record + new single-JSON were both emitting); 4 stock records (Turkish Oud Maple cluster 13355 + IT/FR/ES) set to `_stock_status=outofstock` + `_stock=0` + `_backorders=no` — frontend `availability=OutOfStock` verified in all 4 languages; 3 Caglama excerpts restored to A's `<ul><li><strong>` HTML structure after WebMCP 1.0.14 deploy. **WebMCP 1.0.14 ships two fixes:** (1) `content_update_post` uses `wp_kses_post()` for `post_excerpt` instead of `sanitize_text_field()` — HTML formatting survives writes; (2) `seo_write_meta` partial-update via `array_key_exists` checks — missing args no longer coerced to `''` which had been clearing existing Rank Math meta. **ISSUE-025 canonical** diagnosed and closed: hypothesis (a) confirmed — B's `blog_public=0` triggers Rank Math's auto-suppression of canonical link tag + duplicates the noindex robots header; swap-day `blog_public=1` flip restores it automatically, no theme/plugin/filter override required. **Smoke test 44/44 PASS** post-update (A health, B health, 10 system pages, 4 stock records, 15 FAQ-cleaned, 3 excerpt records, 5 inline-strip persistence samples, 2 redirect probes, hreflang sanity). **Pre-swap content sync 100% complete.** **Operator swap-day checklist:** (a) flip `blog_public=0 → 1` on B after DNS propagates; (b) DNS swap; (c) `wp search-replace tapadum.com new.tapadum.com` (then reverse) to normalize absolute URLs in Elementor data; (d) add `wp-json/luwipress/v1/` to LiteSpeed "Do Not Cache URIs" (TWO-PART REST cache leak doctrine); (e) optional Rank Math redirections export A → import B. **Deferred (post-swap, not blockers):** Pattern J (TASK-054 — 21 page Elementor CTA cleanup awaiting Tapadum JSON patch + page IDs) + WPML hreflang option key (ISSUE-015). **Artefacts:** `temp/migration/MIGRATION-RECOVERY-REPORT-2026-05-11-v2.md` (recovery plan) + `TAPADUM-MIGRATION-FINAL-2026-05-12.md` (final status to operator) + `27_smoke_test.json` (full verification) + scripts `15_discovery.py` through `27_smoke_test.py` (re-runnable). Resume via `.claude/next-session-prompt.md` or `project_tapadum_migration_2026_05_11.md`.

**Earlier (2026-05-11 night — luwipress-gold 1.7.9 UI polish marathon + screenshot system):** **LIVE on new.tapadum.com:** core **3.1.51** + theme `luwipress-gold` **1.7.9** (sha `cb75c33311153b85`, 447 KB). All-day operator feedback loop polished 8 widgets.css areas and shipped a new screenshot system. **widgets.css fixes** (cumulative, all live): (1) mega-menu hover wrap — `padding-left: 6px` → `transform: translateX(6px)` (no layout shift, long labels stay one line). (2) PDP sticky bar ↔ chat — `.lwp-pdp-sticky__inner` padding `14px 32px` → `14px 96px 14px 32px` so CTA clears 56×56 chat launcher. (3) Mobile drawer chips — `justify-content: space-between` → `flex-start`, foot right-padding 20→88px (chat clearance). (4) Mobile topbar collapsed to single row (email + lang only); Track + DHL promo hidden, drawer covers them. (5) Mobile breadcrumb header gap — shorthand `padding: 0 16px !important` was zeroing top:64px, switched to `padding-left/padding-right` longhand. (6) Mobile result-count + sort — added `main.lwp-shop-archive` higher-specificity selectors. (7) PDP gallery overflow — WC Flexslider's inline `width: 594px` clamped via `.woocommerce-product-gallery` chain `width: 100% !important` + `body.single-product main .e-con/.elementor-section { overflow-x: clip }`. Sale badge restyled to absolute brand-gold pill. (8) Cart totals compaction — shipping rows inline, empty `.cross-sells` hidden. **Mega-menu PHP**: `render_mega_panel` 3rd-level `<ul>` render removed; all columns now uniform (Oud's special sub-menus no longer stand out). **style.css**: Theme URI + Author URI → `https://luwi.dev/luwipress`; description rewritten ("AI-powered" → "LuwiPress-powered" + companion ecosystem mention). **functions.php**: new `wp_prepare_themes_for_js` filter appends `?ver={LUWIPRESS_GOLD_VERSION}` to `screenshot.png` URL — operators never see stale screenshot art after a bump again (this fix alone closed a 30-minute "live still shows old" loop). **screenshot.png v2**: 1200×900, real LuwiPress monogram via `mask-image` stencil + brand-gold `#b08930` fill (no more `∞` unicode placeholder), top eyebrow `— LUWIPRESS ECOSYSTEM —`, big wordmark (90px) + tagline (30px), two distinct chip rows — `Ecosystem Plugins` (gold-bordered: LuwiPress Core / WebMCP / Marketplace Sync / Open Claw) + `Friendly With` (gray: Elementor / WooCommerce / WPML / Rank Math / FluentCRM / LiteSpeed), no version (operator preference — never rebuild screenshot per patch). **Screenshot system documented**: source `c:/tmp/screenshot-template.html` + `c:/tmp/luwi-logo.png`; render via `chrome.exe --headless=new --window-size=1200,900 --screenshot=... --allow-file-access-from-files file:///c:/tmp/screenshot-template.html` (NOT chrome-devtools-mcp — Windows DPR=1.25 distortion). See `feedback_headless_chrome_cli_for_screenshots.md`, `feedback_screenshot_cache_bust_filter.md`, `feedback_mask_image_for_logo_recolor.md`. **Final ZIP**: `releases/luwipress-gold-1.7.9.zip` sha-16 **`cb75c33311153b85`** (447 KB) — already uploaded to new.tapadum.com. Session closure protocol applied. Resume by reading `project_session_2026_05_11_1_7_9_ui_polish.md` for the full change log; live verification commands in `.claude/next-session-prompt.md`.

**Earlier (2026-05-11 evening — new.tapadum.com post-migration UI audit + sprint):** Operator migrated to new.tapadum.com and asked for UI audit; the audit caught 15 issues spanning theme CSS, hero placeholder copy, server-side fatals, and image-thumbnail gaps. Sprint shipped via REST + Kit CSS (live now, no upload needed): (a) hero copy patch on page-id 7690 — Brisighella→Istanbul, "Hand-crafted craft"→"Hand-crafted instruments from Anatolia, Persia & the Mediterranean", stats 1/0→25+/26, removed leaked "Hero image · luthier workshop" annotation; pre-edit snapshot `6286b656` saved for rollback. (b) Kit CSS V49 layer — hero left + right CSS migrated from inline `<style>` blocks (which `wp_kses_post` stripped during the widget save, a critical gotcha to remember — see `feedback_wp_kses_post_strips_style.md`). (c) Kit CSS V50 layer — bridge mirroring widgets.css 1.7.9 fixes: tablet menu squeeze 901-1100px, STRING INSTRUMENTS `nowrap`, BLOG hide 901-960px, mobile info-bar `.elementor-widget-wrap` block override. Kit CSS now sits at **416,798 B** — close to historical 412KB truncation threshold, consolidation pass overdue. **Build-unreleased ZIPs awaiting Hostinger upload**: `releases/luwipress-gold-1.7.9.zip` (sha `d22c75dceef33a27`, 436 KB — widgets.css fixes + `03-homepage.json` hero template Brisighella/copy patches) and `releases/luwipress-v3.1.52.zip` (sha `2c3e36c36d65d34e`, 602 KB — chat 429 storm fix; `check_chat_permission` only consumes per-IP rate-limit budget on POST/PUT/DELETE, GET session bootstrap pass-through). **Manual items still pending operator**: (1) install Regenerate Thumbnails plugin OR run `wp media regenerate --image_size=large,medium_large,1024x1024 --yes` for missing subcat hero variants (Darbuka `*-1024x1024.jpg` 404s on desktop). (2) Tail PHP error_log for `ppc-simulate-cart` 500 fatal on every PDP + add `wp-json/luwipress` to LiteSpeed minify exclusion list (currently stripping the `<!--` opener of Meta Pixel inline script → `Uncaught SyntaxError: Unexpected identifier 'Pixel'` → all `fbq('track', 'ViewContent')` events dead). (3) WPML Taxonomy Translation → `Instruments` parent category for IT/ES (eyebrow renders English on translated archives). (4) Translation Manager Quality Audit → trash ES cascade-dup posts (`oud-arabe-vs-oud-turco-…` pair). (5) Edit blog title "Sound Therapy and Meditative **Pover**" → "Power". Audit + sprint notes: `temp/audit-2026-05-11/AUDIT-REPORT.md` + `SPRINT-COMPLETE.md`. **3.1.53 backlog candidate**: extend `set_widget_text` with `kses_pass=false` flag (or pre-extract `<style>` then re-inject) so REST html-widget patches don't silently strip inline styles. Continuation prompt: `.claude/next-session-prompt.md`.

**Earlier-active focus (2026-05-10 overnight — drift sweep working end-to-end + theme polish iterations):** **LIVE:** core **3.1.50** (sha `c2d0685b0abe40d7`) + webmcp **1.0.13** (sha `61b4f4e7a48cf2e7`) + theme `luwipress-gold` **1.7.3** (sha `5983aa0a8ec6fd97`) + 3 Kit CSS overlay layers (V49.2 mobile padding, V50.1 sticky kategori hero, V52 hide loadmore label). **3.1.50 ghost-Elementor routing fix** added a guard in `request_translation()` — when a post has substantial post_content (> 200 chars stripped) but only nominal `_elementor_data` widgets (< 5 widgetType instances), the routing forces the standard non-Elementor path so post_content actually gets AI-rewritten. Confirmed working end-to-end: 60/129 drift posts cleared in 75min on new.tapadum.com (4/4 random quality probes pass — pure target-language bodies, 0 EN stop-word hits). Pipeline pace: 22/hr → 98/hr as cron warmed; remaining 69 posts drain unattended via wp_cron in ~45-60min after session close. Smoke test (single FR post inline, 76s): body `'Buying guide for Clay Darbuka. The mesmerizing rhythms...'` → `'Les rythmes envoûtants de la musique du Moyen-Orient...'`. Theme 1.7.3 also added: mega-menu admin UI for `_lwp_gold_mega_featured` (Görünüm → Menüler → top-level item edit panel), infinite-scroll spinner (textual labels, replaced in 1.7.4), sticky kategori hero, journal spacing baked, theme admin Status tab 7-card layout fix. Theme **1.7.4** ZIP built (sha `a3362bb39ef764bc`) but **not deployed** — replaces 1.7.3 textual loader with **purely symbolic** (rotating ring during fetch, "X / Y" numeric pagination at rest, `✓` end-of-list, `⚠` error) so no i18n strings needed across FR/IT/ES/DE/AR/etc. **Outstanding:** operator uploads 1.7.4 ZIP when ready (drops V52 Kit CSS hack); next theme bump (1.7.5?) should bake V49.2 + V50.1 Kit CSS layers into widgets.css for permanence.

**Pre-3.1.50 state (2026-05-09 evening, drift detector ship):** Built lint-clean: `luwipress` core **3.1.49** (600 KB, 65 files, sha-16 `e72a86463b30a9c4`), `luwipress-webmcp` **1.0.13** (64 KB, 7 files, sha-16 `61b4f4e7a48cf2e7`), `luwipress-gold` theme **1.7.1** (436 KB, 110 files, sha-16 `10cdeb7b9bafe226`). **Drift sweep:** new `/translation/language-drift` REST endpoint scores translated post bodies via 10-language stop-word ratio (target_score = target_hits / (target+source) hits, threshold 0.45); flags translated posts whose body content is still in source language — the silent failure mode where Translation Manager reports 100% coverage but the Spanish blog renders English paragraphs. Live evidence on new.tapadum.com Spanish Oud blog: ES score 0.030 (192 EN vs 6 ES stop-words / 1245 words). New `/translation/force-retranslate` endpoint clears the elementor "already-translated" guard meta (`_luwipress_elementor_translated` + `_chunked` + status meta) + re-fires AI pipeline async via `wp_schedule_single_event('luwipress_force_retranslate_single', [post_id, lang])`. Translation Manager admin grows red-accent "Translation Quality Audit" panel — drifted posts grouped by source, per-language score chips (link to live URL, score % on hover), sweep-all + per-row buttons. Theme Bridge gains `language_drift_sweep` maintenance tool (scan/execute/restore with per-translation body snapshot ledger; restore writes back pre-execution `_elementor_data` + post fields). WebMCP companion gains `translation_language_drift` (read-only) + `translation_force_retranslate` (destructive, openWorld) MCP tools. **Admin design system unification (3.1.49):** `theme-page.php` + `marketplace-settings-page.php` + `open-claw-page.php` + 8 inline hex codes in `settings-page.php` migrated to canonical `lp-header` brand strip + `luwipress-stat-card` semantic accent grids + `lp-pill pill-success/pill-info/pill-warning/pill-neutral` badges + `--lp-*` design tokens (dark-mode-ready). Marketplace companion gains hero stat row (live channels / configured / untouched / adapters); brand-identity dot colours (Amazon orange `#ff9900`, eBay red `#e53238`, Trendyol orange `#f27a1a`, etc.) preserved on purpose because brand recognition matters. `pill-info` helper class added to admin.css cascade. Resume by **manually uploading the 3 ZIPs to new.tapadum.com via Hostinger** then running `wpml_term_repair` (Italian/Spanish kemence-classico 404) + `unwanted_landing_pages` (legacy SEO orphans) + the new `language_drift_sweep` (queue all 4-language blog drift). See `project_drift_sweep_3_1_49_ship_2026_05_09.md`.

**Pre-3.1.49 state (2026-05-08, theme tools framework ship):** `luwipress` core **3.1.48** (600 KB, 65 files, sha-16 `209e1ac703332aff`), `luwipress-webmcp` **1.0.12** (64 KB, 7 files, sha-16 `f126cb730157b658`), `luwipress-gold` theme **1.7.0** (433 KB, 109 files, sha-16 `a41070b093bad6c3`). New `LuwiPress → Theme` admin tab (Status / Tools / Settings) backed by a **Theme Bridge** primitive in `includes/class-luwipress-theme-bridge.php`; themes register tools/settings via two filters (`luwipress_theme_tools` + `luwipress_theme_settings`). Active theme registers **23 maintenance tools** covering the Elementor + WC + WPML + SEO + Redirects axes: `elementor_shell_cleanup`, `slug_conflict_audit`, `wpml_translation_drift`, `legacy_canvas_migration`, `kit_css_health`, `orphan_media_scan`, `wpml_structure_sync`, `unwanted_landing_pages`, `subcategory_template_parity`, `triangle_health`, `wpml_term_repair`, `menu_translation_propagate`, `product_translation_completeness`, `wc_template_assignment`, `broken_internal_links`, `empty_term_archives`, `wpml_string_translation_pending`, `page_speed_signals`, **`canonical_audit`**, **`hreflang_reciprocity_audit`**, **`redirect_chain_detector`**, **`sitemap_indexation_parity`**, **`seo_triangle_health`** (last 5 = SEO Audit Pack). Each tool exposes scan/execute/restore primitives; WPML-aware tools auto-expand trid siblings; backups persist 20-deep in `luwipress_theme_tool_backups`. **KG Action Queue integration** — bridge subscribes to new `luwipress_kg_action_queue_external_candidates` filter so theme-tool findings appear alongside core enrichment/SEO opportunities with severity-graded ROI (cached 1h; busts on `luwipress_theme_tool_executed` action). **Elementor Pro template force** — `pdp_template_id` / `archive_template_id` theme_mods override Elementor's own conditions resolution at `elementor/theme/get_location_templates/template_id` (fixes WPML duplicate-template wars). **Strict-404 banner** for missing translated subcategories + **410 Gone** for orphan-listed pages (both opt-in via theme_mod). **Hero stat-bar** on Status tab shows 7 metrics (theme/companion/tools/findings/untranslated/backups/kit-css-headroom). **Reset-to-defaults per group** on Settings tab. WebMCP companion adds 6 new MCP tools (`theme_tools_list`, `theme_tool_run`, `theme_tool_restore`, `theme_tool_backups`, `theme_settings_get`, `theme_settings_set`). Wizard `tool_scan` / `tool_execute` proxy step handlers added in `inc/wizard/bootstrap.php`. New REST namespace surface: `/luwipress/v1/theme/{tools,tools/scan,tools/run,tools/restore,tools/backups,settings,status}`. New theme settings group `wpml` (4 settings: block_orphan_landings, wpml_subcat_strict_404, pdp_template_id, archive_template_id). Resume by **manually uploading the 3 ZIPs to new.tapadum.com via Hostinger** then running the verification checklist in `.claude/next-session-prompt.md` (covers `/it/.../kemence-classico/` 404 repair via `wpml_term_repair` + IT/EN orphan landing trash via `unwanted_landing_pages`). See `project_theme_tools_3_1_48_ship_2026_05_08.md` + `project_theme_tools_architecture_2026_05_08.md`.

**Pre-1.7.0 state (2026-05-07/08 marathon, folded into 1.7.0):** `luwipress-gold` source was **1.6.4-pending** — version header still says `1.6.3`, no ZIP built, not synced to theme repo, not deployed. Session 2026-05-07/08 added: WPML/Polylang cross-language slug-collision discovery (`icl_translations` JOIN + `pll_get_post_translations` for Polylang) in `inc/template-redirects.php`; render-time URL rewrite in mega menu (`resolve_to_product_cat()` + `resolve_target_url()` with cross-language map fallback) so menu HTML directly emits `/product-category/...` instead of legacy page URLs; branded `woocommerce/cart/cart-empty.php` + `is_empty()` guard in `cart.php` (closes blank-middle bug operator screenshotted on /cart/); server-side page loader (`inc/page-loader.php` at `wp_body_open` p1 + `lwp-booting` class via `wp_head` p1 inline script — fixes flash-of-content-before-loader); shop-by-category 5th footer column auto-generated from `product_cat` (Customizer toggleable); payment-row brand-prefix dedup ("WooPayments (Card)" + "WooPayments (Alipay)" + ... → single "WooPayments" pill with tooltip); default trust signals when WC active; Customizer fallback wired into mega menu widget `render()` + `header.php` (replaced hardcoded `4`/`auto`/`true` defaults); detected skip-slug list (`luwipress_gold_skip_redirect_slugs()` from `wp_page_for_privacy_policy` + `wc_get_page_id()` + `page_on_front`/`page_for_posts`) replacing hardcoded `('','about','contact','privacy','terms')`; loader Customizer toggle in Performance section. Resume by syncing source diff to `c:/tmp/luwi-themes-inspect/luwipress-gold/`, bumping to 1.6.4, building, deploying. See `project_luwipress_gold_1_6_4_session_2026_05_08.md` + `feedback_wpml_cross_lang_slug_conflict.md` + `feedback_detected_skip_slugs.md` + `feedback_server_side_loader.md` + `feedback_wc_cart_empty_branch_required.md` + `.claude/next-session-prompt.md`.

**new.tapadum.com live state (verified 2026-05-08 via MCP):** theme version **1.6.3** is deployed (CLAUDE.md previously said 1.6.1; 1.6.3 had been pushed before this session). `theme_mod luwipress_gold_resolve_slug_conflicts = true` is now ON (was opt-in default-off in 1.6.0 source); cache transient was stale at session start, `cache_purge` rebuilt it. Result: 31 EN + 12 IT slug collisions auto-discovered and 301-redirecting to `/product-category/...`. ES/FR translated-page collisions (`/es/percusiones/`, `/es/vientos/`, `/fr/vents/`) and 5 lone-wolf pages (Santur, Black Sea Kemence, Classical Kemence, Armenian Duduk, Saz Baglama Accessories) STILL fall through to legacy pages on 1.6.3 — the cross-language WPML JOIN + Levenshtein-1 fuzzy + menu-parent inheritance passes added in **1.6.4 source** close every gap. After 1.6.4 deploy + cache purge, EVERY menu item lands on `/product-category/...` (universal slug hegemony). Slug-conflict module now ships 5 discovery passes: exact → cross-language → plural+prefix fuzzy → Levenshtein-1 → menu-parent inheritance.

**Pre-1.6.4 source (2026-05-06 LATE night sprint):** `luwipress-gold` theme **1.6.3** — template-kit healing pass on top of the 1.6.0 mobile-spec sprint. Late-evening session shipped four patch ZIPs in succession addressing operator-surfaced bugs: hub redirects, slug-collision fuzzy match, smart filter sidebar (replaced redundant Categories list), blog page auto-promote, Elementor canvas template (fixes "some old posts won't open" symptom from theme migration), parent-term archive enrichment, footer enhancements (SVG socials + newsletter + trust + payments + Customizer panel), mega menu Customizer panel + auto-inject blog categories, archive chip strip default-OFF, "You may also enjoy" duplicate suppression. Files: `inc/template-redirects.php` + `inc/archive-parent-enrichment.php` + `inc/layout-fixes.php` + `inc/smart-filters.php` + `inc/blog-page-fallback.php` + `inc/footer-enhancements.php` + `inc/mega-menu-customizer.php` + `elementor_header_footer.php` (NEW root template) + edits to `archive.php` + `footer.php` + `inc/widgets/lib/class-mega-menu.php` + `inc/ai-surface.php` + `inc/wizard/lib/class-detector.php` + `inc/wizard/lib/class-importer.php` + `inc/wizard/views/wizard.php`. ZIPs: 1.6.0 (336 KB, `30c07240c5e93e32`), 1.6.1 (354 KB, `27aeac5508554d51`), 1.6.2 (367 KB, `7d0590f559b34e9a`), **1.6.3 (367 KB, `83b960c2f6f82db4` — DEPLOYED to new.tapadum.com)**. New Customizer paths: LuwiPress Gold → Performance (slug-collision toggle) / Mega Menu (threshold + columns + counts + blog auto-inject) / Footer (9 socials + newsletter + 3 trust signals + payment row). New term-meta UI: Ürünler → Kategoriler → Edit → "Atelier note" + "Featured product (override)". See `project_luwipress_gold_1_6_3_sprint_2026_05_06.md` + `feedback_smart_filter_sidebar_pattern.md` + `feedback_elementor_canvas_template_required.md` + `feedback_slug_collision_fuzzy_match.md`.

**Pre-1.6.3 state (paused; integrated into 1.6.3):** `luwipress-gold` theme source at **1.6.0** with full Mobile Spec Preview v2 implementation (parallel session): 4 new PHP templates + 5 PHP rewrites + 4 JS controllers + 1240-line @media CSS + drawer search removed in dual-search UX consolidation. Templates: `404.php` (editorial), `search.php` (sticky bar + tabs), `page-contact.php`, `page-about.php`, `single-master.php`. Patches: `single.php` reading-progress hook, `archive.php` cat-chips (now opt-in via filter in 1.6.3), `dashboard.php` greeting wrapper, `cart.php` + `form-checkout.php` sticky bars. JS controllers: `initReadingProgress()`, `initAudioCards()`, `initReasonChips()` + drawer `cleanLabel()` decorator stripper. New filter API surface: 12 `apply_filters('luwipress_gold_*')` hooks for content-array overrides. Two new Elementor kit JSONs: `08a-journal-archive.json` + `08b-journal-single.json` (Theme Builder Archive + Single Post). See `project_luwipress_gold_1_6_0_2026_05_06.md` + `reference_mobile_spec_preview_v2.md` + `feedback_dual_search_ux_decision.md` + `feedback_template_filter_api_pattern.md`.

**Earlier-active focus (paused 2026-05-06 evening):** `luwipress-gold` theme **1.5.3** sprint (atelier-ledger mobile drawer + z-index hardening + mobile header single-row 3-grid) + core **3.1.47** (KG Autopilot D.3 + signal layer D.2 + theme companion contract D.1). ZIPs ready: `releases/luwipress-gold-1.5.3.zip` (sha `bae4523574d7b86d`, 294 KB) + `releases/luwipress-v3.1.47.zip` (sha `203025a09b5ce1ef`, 568 KB). Deploy to new.tapadum.com **partially verified during session**: 1.5.1 deployed, drawer header overlap (1.5.2 fix) and 2-row header wrap (1.5.3 fix) reported by operator from live screenshots. **1.5.2:** drawer/scrim z-index 100010/100005 (above WP admin bar 99999) + `body.lwp-drw-locked` class drops sticky headers + admin bar to z-index 1 with pointer-events:none while drawer open. **1.5.3:** mobile (<=900px) `.lwp-site-header-inner` switched to `display:grid; grid-template-columns: auto 1fr auto` — hamburger left col, logo center col with `max-width:60vw + max-height:36px` cap, action icons right col with `flex-wrap:nowrap`; mega-menu inside header inner explicitly hidden. Matches the **Mobile Spec Preview** `.ch-top` design-handoff pattern (Claude Design URL `L2PLMPlWdeL6xb2kjVE-pQ`). **1.5.0 (5 UI feedback fixes):** float bar↔footer IntersectionObserver handoff, header menu typography normalisation, mega panel viewport-aware (`width: min(1180px, calc(100vw - 48px))`), full mobile responsive layer (initial generic drawer), product card meta padding inset. **1.5.1 (Tapadum design handoff):** atelier-ledger mobile drawer (warm cream `--bg-alt`, paper-grain dots, italic-serif categories, ±28px circular plus/minus glyph, AI search field, 3-col utility row Account/Cart/Track with sale-red badge, Atelier pick featured-product card auto-pulled from page or `window.LuwiGold.drawer.pick` override, warm-deeper foot with lang/social/contact, stagger entrance, prefers-reduced-motion fallback) + hamburger inject across all header rendering paths (Gold fallback / Elementor Pro / generic) + mega menu walker WPML/Polylang strip. **Repo split done (option A):** theme moved to its own repo `umutsun/luwi-themes` (main `69130b4`, tag `gold-v1.5.1`); core repo has `themes/` in `.gitignore`. Push core via HTTPS direct URL (`git push https://github.com/umutsun/luwi-press.git master`) — SSH alias `github-umutsun:` doesn't resolve from bash. Theme local workflow: edit `themes/luwipress-gold-elementor/`, sync to `c:/tmp/luwi-themes-inspect/luwipress-gold/`, commit + push from there. See `project_luwipress_gold_1_5_1_2026_05_06.md` + `feedback_two_repos_theme_split.md` + `feedback_design_handoff_implementation.md` + `.claude/next-session-prompt.md`.

**Earlier-active core focus (paused for now):** `luwipress` core stayed at **3.1.44**. Tapadum live: 3.1.43 (legacy) / 3.1.43 (new.tapadum.com) + Kit CSS sha `73aa6964f688e486`. Core feature roadmap (translation outdated detection 3.1.41+, etc.) un-paused once theme work consolidates.

